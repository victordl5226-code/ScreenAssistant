# Backlog de mejoras — ScreenAssistant

Backlog de mejoras pendientes registrado por el equipo (Arquitecto / QA / UI-UX).
Fecha de creación: 31/07/2026. Estado: abierto.

---

## En curso

- **Lote 12 — Gate mecanizado (M13) + Tests de migración Room (M6)**: CERRADO (04/08/2026, docs/DISENO-LOTE12-GATE-MIGRACIONES.md — acta de cierre en §8.2). **803 tests verdes / 0 fallos** (base 795 + 8; conteo por XML de resultados). Excepción de regla autorizada ("sin Robolectric ni deps nuevas") SOLO para los artefactos del lote. **M6 — migraciones Room (core:data, JVM + Robolectric, corre en el gate)**: `exportSchema = true` (1 palabra, cero impacto runtime) + `room.schemaLocation` (KSP) + **P1** `sourceSets["test"].assets.srcDir("$projectDir/schemas")` (sin esto MigrationTestHelper lanza FileNotFoundException — verificado: los 3 tests T1/T2/T3 resuelven los JSON); bootstrap controlado de `2.json` (build temporal v2: entities/version + alarmDao() comentado para que KSP compile — único detalle de procedimiento extra; revertido) con control NEGATIVO ("alarms" ausente) y POSITIVO ("memories"/"pending_messages" presentes); `AppDatabaseMigrationTest` (3: identidad de esquema contra 3.json, preservación de 4 filas con PK/valores/orden, tabla alarms funcional contra el DAO — cubre @Upsert) + `MessageDaoTest` (5: insert+emisión, orden ASC, delete/no-op, clearAll, round-trip nullable) — todos `@RunWith(RobolectricTestRunner::class)` (NO AndroidJUnit4) + `@Config(sdk = [34])` + `inMemoryDatabaseBuilder` SIN `allowMainThreadQueries()`. **M13 — gate mecanizado**: JaCoCo **0.8.13** por módulo (core:domain tareas nativas; app/core:data/feature:overlay/service:system custom con `dependsOn("testDebugUnitTest")` interno — P2-3 — y exclusiones de generados incl. `*_GeneratedInjector` — P2-2 —; verificado: 0 clases Hilt/Room/KSP en los 5 reports); **Fase A (medir)** con el estado final de Fase 1 (+8 tests) y el MISMO comando canónico (`gate --rerun-tasks`), cero tests nuevos entre A y B; **Fase B (fijar)** umbrales = medición − 0.05: **core:domain 91.02% → 0.86**, **core:data 62.63% → 0.57**, **service:system 65.07% → 0.60**, **feature:overlay 37.22% → 0.32** (mide BAJO el suelo sugerido 0.45–0.50 → regla de no-ruptura: umbral = medición − 0.05; subir la cobertura del overlay es DEUDA registrada), **:app 11.07% REPORT-ONLY sin enforcement (P2-1)** — excluida del gate, report disponible `gradlew :app:jacocoTestReport`; tarea raíz `gate` (tests JVM + assembleDebug + verification de los 4 módulos con umbral; `:app:connectedDebugAndroidTest` queda como tarea separada documentada — requiere dispositivo). **androidTest (+8, solo dispositivo)**: deps androidx.test (ext:junit 1.2.1, runner 1.6.2, rules 1.6.1; sin espresso ni hilt-testing) + `ApiKeySectionTest` (5, createComposeRule — QA-5) + `MainScreenTest` (3, createAndroidComposeRule<ComponentActivity>) — todos envueltos en `ScreenAssistantTheme` (QA-4), sin asserts sobre el badge de accesibilidad (Settings.Secure real); `compileDebugAndroidTestKotlin` VERDE (no corren sin dispositivo; guion en el diseño §5.4). **Catalog (Lote 10)**: robolectric 4.14.1, androidx-test-core 1.6.1, ext-junit 1.2.1, runner 1.6.2, rules 1.6.1, jacoco 0.8.13. **Desviaciones**: overlay bajo su suelo (deuda de cobertura — **META: ≥0.45, umbral actual 0.32; recalibrar al alza en el lote que suba cobertura del overlay, y atacarlo con tests de OverlayViewModel/CharacterState/TTS restantes**); primera ejecución de Robolectric descarga android-all ~150 MB (R1, una sola vez, +10 min en la primera corrida del gate); `assertExists` NO existe en compose-ui-test 1.7.x → `performScrollTo().assertIsDisplayed()` en MainScreenTest. **Fuera de alcance (documentado en el diseño §9)**: fallbackToDestructiveMigration (builder de :app), migración 1→2 (no existe), umbral JaCoCo de :app, CI con emulador, bumps de versiones.

- **Lote 11 — Sanitización de mensajes de error (M3 streaming + puente Tasker + constante M1)**: CERRADO (03/08/2026, docs/DISENO-LOTE11-ERRORES.md). **795 tests verdes / 0 fallos** (base 791 + 4). **M1 — constante del temporizador**: `TIMER_ERROR_MENSAJE` en el companion de SystemCommand (junto a TIMER_MIN/MAX_MINUTOS), usada por el parser (rama 3) y TimerAction (guard defensivo) — fuente única; el wire NO la usa (formato `valor_invalido` con valor recibido — canales distintos); 5 tests que fijan el literal SIN editar. **M3 — streaming (GeminiRepositoryImpl)**: el Lote 8 pagó la estructura (emit en catch IOException, else throw); el residuo eran los literales `"Error en streaming: ${e.message}"` (emit de recolección y flowOf de construcción) → constante top-level `FALLO_TECNICO_GENERICO` ("He tenido un problema técnico momentáneo. Inténtalo de nuevo en un segundo." — byte-idéntica a la de sendMessage, deriva cero entre caminos) + `Log.w` con detalle; `mockkStatic(Log)` añadido en GeminiRepositoryTest (core:data sin `isReturnDefaultValues` — P1-1 del shift-left, sin esto el gate era imposible); +3 tests (catch de construcción, CE sendMessage, CE construcción), 1 editado (el assert "red caída" → constante). **Puente Tasker — SANEAR (decisión del Arquitecto: el wire tiene VOZ — TTS por defecto vía `hablar = !silencioso` → ADR-009 aplica al canal humano, el diagnóstico se preserva en logcat y en el código máquina `fallo_ejecucion`)**: `MSG_FALLO_EJECUCION = "Error: No pudo completarse la acción."` en TaskerBridgeContract (mismo texto humano que M10 — byte-idéntico entre camino funcional y excepcional), usada en los 3 call sites (TaskerMessageHandlerImpl:68, SystemCommandBridgeImpl:71, TaskerCommandReceiver:77 `t.message` H9); muere el `?: "Error desconocido"`; `fallo_accion` y `ActionResult.Error` verbatim INTACTOS (ADR-B7). **EXTRA-1 (B5, hallazgo del Arquitecto)**: `SystemCommandBridgeImpl` tragaba `CancellationException` (con withTimeoutOrNull, la cancelación por timeout se tragaba y la corrutina seguía → `fallo_ejecucion` en vez de `error_timeout`, H6 roto) → rethrow CE antes del catch genérico; +1 test de propagación (try/catch manual, P2-1 — nunca assertFailsWith sobre suspendida). **Decisión A (B5, Orquestador tras QA P2-2)**: `sendMessage` y el catch de CONSTRUCCIÓN de streamMessage tragaban CE (solo la recolección re-lanzaba vía `else throw`) → 2 rethrows + 2 tests. **Saneo extra (hallazgo del Supervisor en el cierre)**: `SpeechToTextManager.kt:43` interpolaba un código Int crudo del SpeechRecognizer al canal visual del overlay → "Error desconocido al escuchar." (1 línea, sin tests que lo pinen). Verificación de cierre: grep `e\.message`/`t\.message` en src/main → solo `Log.w(…, e)` locales (EncryptedPrefsStore/PuenteConfigStore), comentarios y falsos positivos (`actionResult.message`/`result.message`); `"Error en streaming"`/`"Error desconocido"` → 0 en src/main. **Diferidos**: M6 (tests Room — exige Robolectric/instrumentación), M13 (JaCoCo + androidTest — infra de build). **Pendiente operativo**: smoke en dispositivo (Tasker con acción que falle → TTS dice el genérico, detalle en logcat; comandos de voz M1/M2 del Lote 10; overlay tras D1/M15; captura API 30+ tras D7/EXTRA).

- **Lote 10 — Deuda menor restante M1–M26**: CERRADO (03/08/2026, docs/DISENO-LOTE10-MENORES.md). **791 tests verdes / 0 fallos** (base 777 + 14). **M24 — Naming + DI + catalog** (PRIMERO): `GeminiRepository`→`GeminiRepositoryImpl` (core/data/remote) y `MemoryRepository`→`MemoryRepositoryImpl` (core/data/repository) con `git mv` (clases homónimas renombradas; los consumidores usan las interfaces → solo AppModule y GeminiRepositoryTest tocados); los 3 @Provides FQCN de AppModule ELIMINADOS → 4 @Binds en DataModule (bindGenerativeModelFactory/bindMemoryRepository/bindGeminiRepository/bindScreenContextRepository — las impls viven en core:data, el binding les pertenece); `gradle/libs.versions.toml` CREADO (catalog mínimo: room 2.6.1, junit 4.13.2, mockk 1.13.12, coroutines-test 1.9.0 — solo las deps duplicadas reales; migración gradual del resto documentada en el toml; `gradlew help` OK con Gradle 9.3.0); migrados los 5 build.gradle.kts (app, core:data, core:domain, feature:overlay, service:system). **M1 — Invariante SetTimer 1..1440**: constantes `TIMER_MIN_MINUTOS`/`TIMER_MAX_MINUTOS` en `SystemCommand` (KDoc del invariante); `AccionRegistry.poner_temporizador` consulta las constantes (mismo valor → CorrespondenciaWireTest/SystemCommandJsonCodecTest INTACTOS); parser rama 3 valida antes de ejecutar (fuera de rango → "Error: La duración debe estar entre 1 minuto y 24 horas." SIN ejecutar, precedente B2); guard ANTES del try en `TimerAction.setTimer` (sin startActivity); +8 tests (parser +6: 0 min, 5000 min, 1441 min, 24h→1440, 1 min→1 [P1-2 boundary bajo], "un rato"→null [P2-1]; TimerActionTest +2: guard 0/1441 con verify exactly=0 de startActivity). **M2 — Cortesía en llamadas**: `callOrNumber` limpia el sufijo `(?i)\s*(?:por\s+favor|porfavor|porfa)\s*$` ANTES del phoneRegex/contacto; guard "llama a por favor" → "Error: ¿A quién quieres que llame?" sin ejecutar; +5 tests (600 123 456 por favor→CallNumber, 600 123 456 porfavor→CallNumber [P1-1], Ana por favor→Call, al jefe por favor→Call, "llama a por favor"→error); regresión "llama al jefe"/"llama a 600 123 456" VERDE sin tocar. **M10 — Mensaje saneado**: catch externo de SystemActionHandler ya NO filtra `e.message` → Log.w + `ActionResult.Error("No pudo completarse la acción.")`; 3 tests editados (Error("boom")→mensaje saneado) + 1 nuevo P2-3 (SQLiteException interno no se filtra); B5 intacto (22 comandos, CancellationException re-lanzada antes). **M15 — Overlay limpio**: `screenContextRepository` fuera del constructor de OverlayViewModel y del passthrough del servicio (el use case lo lleva); `isNetworkAvailable = true` muerta eliminada; `OverlayUiState.currentOutfitRes` eliminado (cero lecturas; CharacterState.currentOutfitRes se MANTIENE con su test); OverlayViewModelTest sin el mock (24 tests intactos). **M18**: KDoc fail-soft v1.2a declarado en `PuenteConfigStore.guardar()` y `ApiKeyProvider.storeApiKey()`. **M20**: KDoc de `_viewModelStore.clear()` corregido (no-op formal; limpieza real = DisposableEffect de la UI; store se mantiene por setViewTreeViewModelStoreOwner; NO registrar los VMs — veto). **M22**: `TIMEOUT_MS` ÚNICA en TaskerBridgeContract (definición duplicada de AlarmReceiver eliminada; KDoc del contrato actualizado); `NetworkUtils.kt` ELIMINADO (cero call sites; grep → solo docs + comentario documental en SystemActionHandler.hasNetwork sobre la diferencia ETHERNET). **M23**: premisa falsa corregida en KDoc de NoteAction + ADR-003 enmendado (razón = contrato distinto, no capas; 3er consumidor no alcanzado). **M25-KDoc**: `hour ?: 0` de AlarmAction documentado como red de contrato de API cubierto por test (ningún canal produce medio-null; NO eliminar — veto); catch de NoteAction anotado (NO eliminar — veto). **EXTRA-1**: `@Volatile` en `capturaEnCurso` de ScreenContextService (+KDoc cross-thread). **EXTRA-2**: `registrarProveedor()` extraída y compartida por onServiceConnected/onRebind (guard de reconexión). Diferidos documentados: M6 (migraciones Room — room-testing exige instrumentación; riesgo bajo: CREATE TABLE idempotente + fallback destructivo; aclaración: MessageDao usa @Insert/@Query, NO @Upsert) y M13 (JaCoCo/androidTest — infra sin pipeline que la ejecute). M26 CERRADO (verificado, 0 trabajo). Desviación vs diseño §7: gate 791 (no 787) — QA shift-left añadió P1-1/P1-2/P2-1/P2-3 (todos baratos y pedidos en la tarea): +4 sobre el objetivo del diseño. Gate: `.\gradlew.bat compileDebugKotlin compileDebugUnitTestKotlin` ✓, `.\gradlew.bat testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks` ✓.

- **Lote 9 — Pago de la deuda estructural D1–D7**: CERRADO (03/08/2026, docs/DISENO-LOTE9-DEUDA.md). **D5**: `compileOnly` → `implementation` de serialization-json en :app (1 línea, contrato de compilación explícito). **D6**: `provideMessageQueueManager` ELIMINADO de AppModule — vía única `@Inject constructor` + `@Singleton` de clase (más idiomático que el `@Singleton` al provider del diseño; precedente O2/PuenteConfigStore). **D2 — ELIMINADO feature:chat** (veto a integrar): borrados ChatViewModel/ChatScreen/ChatViewModelTest (−5), NavGraph de service:system (dir navigation/ completo), SendMessageUseCase+Test (−3), Message/ChatMessage, ConversationRepository+Impl, `include(":feature:chat")` de settings; editados AssistantOverlayService (sin chatViewModel/conversationRepository), OverlayViewModel (constructor sin ConversationRepository — M15), OverlayUiState (sin messages), OverlayViewModelTest (sin el mock; 24 tests intactos), AppModule (sin provideConversationRepository). NO tocados MessageQueueManager/MessageDao/PendingMessageEntity (cola SMS del puente Tasker — no confundir con el chat). **D1 — MOVIDO AssistantOverlayService → feature:overlay** (package `com.screenassistant.feature.overlay`; vetos a interfaces respetados): manifest del servicio a feature:overlay (verificado en merged_manifest), 4 strings de notificación movidas (service:system queda sin res — P0-1), feature:overlay += lifecycle-service + work-runtime-ktx, MainActivity con el nuevo import/Intent. **D4 — EncryptedPrefsStore** (core:data/util, fuente única del patrón cifrado AES256_GCM+fallback): create/getString/getBoolean/putString/putBoolean/remove/edit{}/isUsingFallback; ApiKeyProvider y PuenteConfigStore como cáscaras finas con API INTACTA y lazy en el consumidor; `guardar()` con UN ÚNICO apply() (P0-2; PuenteConfigStoreTest 8 pasa SIN editar); +3 EncryptedPrefsStoreTest +3 ApiKeyProviderTest (incl. B4: sembrar 1 vez, no re-sembrar tras clear). **D3 — PAGO PARCIAL (veto a unificar)**: GeminiFunctionCatalog (core:data/remote) con declaraciones + nombres + `wirePorNombre` (P1-3: el test NO duplica la tabla) + save_memory→recordar_dato documentado (P1-4); GeminiRepository consume el catálogo; +2 CorrespondenciaGeminiWireTest (paridad UNIDIRECCIONAL: función sin wire → rojo; wire sin función → verde). **D7 — Auto-registro ScreenContextService** (residuo B3): `ScreenContextRepositoryImpl` sin provider en constructor + `setScreenCaptureProvider` público nullable @Volatile (P1-5: NO se ensucia la interfaz de dominio); servicio se auto-registra en onServiceConnected y se desregistra en onUnbind/onDestroy; ELIMINADOS companion estático (WeakReference/instancia()/lastScreenText — MUERTO), ServiceScreenCaptureProvider y provideScreenCaptureProvider; fail-soft intacto (B3); ScreenContextRepositoryImplTest 2 adaptados + 2 nuevos. **DESVIACIONES** (anotadas en el diseño): (a) service/system/build.gradle.kts también perdió las deps AndroidX que quedaron demostradamente muertas tras D1+D2 (navigation-compose, lifecycle-service, lifecycle-runtime-compose, work-runtime-ktx, BOM compose/ui/material3 + plugin compose + buildFeatures.compose — grep previo: 0 usos) → grafo final core:domain + core:data + core-ktx + serialization-json + Hilt; (b) el `when` de ejecución de function-calls se mantiene inline en GeminiRepository (opción "mínimo" del diseño §3.3; el mapeo nombre→wire está en el catálogo). **GATE (03/08/2026): `compileDebugKotlin compileDebugUnitTestKotlin` ✓ y `testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks` → BUILD SUCCESSFUL — 777 tests verdes / 0 fallos** (objetivo EXACTO del diseño: 775 − 8 + 10 = 777; desglose real por XML: app 20, core:data 46, core:domain 445, feature:overlay 38, service:system 228).

- **Lote 8 — Saneamiento (bugs B1–B7 + menores baratos)**: CERRADO (03/08/2026). **B1**: rama 12 re-anclada (`substringAfter("alarma")` + etiqueta re-anclada en ORIGINAL — "pon la alarma a las 7"→SetAlarm(7,0), "la alarma de las 7:30", "activa la alarma...", "pon la alarma para las 8", regresión "pon una alarma para las 7:30 para despertarme"). **B2**: function-call set_alarm valida hora/minuto (inválido → "Error: Hora o minuto inválidos." SIN ejecutar) + guard en AlarmAction.setAlarm. **B3**: captura de pantalla REAL — nueva interfaz `ScreenCaptureProvider` (core/domain), `ScreenContextService` reescrito (suspendCancellableCoroutine, fail-soft null, hardwareBuffer.close en finally, JPEG q80 scale 1024, `instancia()` en companion, sin estáticos), `ServiceScreenCaptureProvider`, `ScreenContextRepositoryImpl` delega. **B4**: el sembrado de la API key sale de AppModule (efecto secundario del @Provides que re-siembra tras "Quitar key") → `sembrarDesdeBuildConfig` con flag `api_key_seeded` (persistente) llamado en App.onCreate; "Quitar key" YA persiste. **B5**: CancellationException re-lanzada en SystemActionHandler y en las 11 acciones con catch propio (incl. ambos catches de openYouTube). **B6**: `@Synchronized`/`@Volatile` en TextToSpeechManager (carrera speak-IO vs onInit-binder sobre ArrayDeque). **B7**: `ResultadoWire.estadoDe` (ver ADR-B7). Menores: **M3** streamMessage IOException→emit estructurado; **M4** CorrespondenciaWireTest contra `AccionRegistry.todosLosWires` (detecta ADICIONES); **M5** restoreActiveAlarms con aislamiento POR FILA; **M8** MAX_TEXT_LENGTH=4000 + recycle solo pre-TIRAMISU; **M9** Uri.encode en openYouTube (2 URIs) + fallback protegido sin excepción cruda; **M11** ADJUST_TOGGLE_MUTE en API 31+ con sdkInt inyectable; **M12** 18 strings muertas eliminadas (app_name/accessibility_description conservadas — la segunda usada en accessibility_service_config.xml); **M16** índice del UI con módulo sobre `outfitCount`; **M19** fallback de idioma TTS (LANG_MISSING_DATA → retención y drenado al instalarse; validación de la carrera B6 manual en dispositivo — KDoc); **M21** captura DIFERIDA: texto de pantalla siempre, imagen solo si parse()==null; **M22** ConnectivityWorker eliminado (archivo + scheduling); **M25** catch unificados en NoteAction; **M26** stub `coEvery(any())` endurecido (tipado), 3 casos de voz fijados (null→Gemini: "a las 7 y media", "temporizador de 5" sin unidad, "las 7") y fakes manuales de ChatViewModelTest migrados a MockK (+testImplementation mockk en feature:chat — misma versión del repo). **GATE VERDE (03/08/2026)**: `:core:data:testDebugUnitTest :service:system:testDebugUnitTest :feature:overlay:testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks` → **BUILD SUCCESSFUL, 775 tests / 0 fallos** (cifra REAL por XML, todos los módulos: app 20, core:data 36, core:domain 448, feature:chat 5, feature:overlay 38, service:system 228; el 748/753 era el conteo incompleto sin :app/:chat). **Ronda de correcciones final (QA/UI-UX, 03/08)**: M12 migración COMPLETA de UI a strings.xml (MainScreen + AssistantOverlayUI, 0 hardcodes), M14 cadena de permisos de 1 pulsación (onResume + startPending), M17 clamp del drag con currentWindowMetrics, M7 disconnect en finally + log sin key, M22 +cancelUniqueWork("connectivity_worker") para instalaciones existentes, M19 fallback de idioma TTS, +2 tests (set_alarm VÁLIDO en GeminiRepositoryTest, query null en MediaActionTest). **+31 tests añadidos** (P0 5, P1 6, P2 2, P3 9, P4 3, P5 1, P6 4, P7 0, P8 3) — el objetivo del plan (776) se ajustó en ejecución: P0 7→5 (2 regresiones absorbidas por tests existentes ya cubiertos), P6 MediaAction +3→+2 (el plan asumía getVolumen/setVolume que NO existen; M11 es de SystemVolumeAction), M5 resultó ser restoreActiveAlarms (no "fallback silent"), P7 sin tests (M12/M22)
- **Lote 7 / Fase 3A — Cierre del puente Tasker (allowlist real, privacidad de respuesta, URL callback AutoRemote)**: DISEÑADO por el Arquitecto y **APROBADO CON CAMBIOS por QA shift-left (02/08/2026 — v1.1 CERRADO, docs/DISENO-LOTE7-CIERRE-PUENTE.md, ADR-015)**: F1 allowlist con origen REAL vía `PendingResult.getSentUid()/getSentPackage()` (API 28+, guard minSdk 26 — corrige la afirmación FALSA "solo en ordered" de ADR-014/T5), política allow-all por defecto + opt-in fail-closed con error aditivo `origen_no_autorizado` (6 claves, sin ejecutar el bridge) e IGUALDAD EXACTA de paquetes (H2); F2 `setPackage` con default `net.dinglisch.android.taskerm` materializado en `PuenteConfigStore.cargar()` (H1 — corrige la afirmación FALSA "setPackage rompería AutoRemote" de ADR-014/T4); F3 URL callback `https://autoremotejoaomgcd.appspot.com/sendmessage` (HttpURLConnection JDK, cero deps, INTERNET ya en manifest, fire-and-forget IO, best-effort sin reintento; `startsWith` del prefijo SOLO en F3, H2); UNA config compartida `PuenteConfig` + `PuenteConfigStore` en **core:data/util** junto a ApiKeyProvider (H3: security-crypto no se propaga a service:system → cero cambios de build) leída por handler/emitter/callback/UI; UI `PuenteSettingsSection` (patrón ApiKeySection; +verticalScroll en MainScreen — M1); seam `TaskerMessageHandler.handle(extra, OrigenTasker)` CAMBIADO (anunciado en el KDoc del Lote 6; 14 tests editados — M2); ~48 tests JVM nuevos/editados (VM ~8-10 — M4; ver M5/M6), sin Robolectric ni dependencias nuevas; 5 providers nuevos en AppModule (O2). **ESTADO (v1.2a): IMPLEMENTADO y GATE VERDE (03/08/2026) - rediseño F1/F3 tras veto (B1/H1); enmienda QA shift-left v1.2a APLICADA (02/08/2026 — H1–H4, §12 del diseño)**: H1 VM 10→8 (T3/T4/T9/T10 eliminados; PuenteError y campo error eliminados), H2 urlCallback 0 INTACTOS (6 conservados editados con flag=true), H3 desglose real por archivo, H4 +2 tests trim/case handler (22) → **gate objetivo 740 CUMPLIDO (03/08/2026): BUILD SUCCESSFUL, 740 tests / 0 fallos (store 8, handler 22, extractor 22, emitter 9, urlBuilder 6, urlCallback 9, VM 8); Fase 1 (core/domain) SIN CAMBIOS; TaskerOrigenVerifierTest (15) ELIMINADO; cero referencias residuales a OrigenTasker/allowlist/origen_no_autorizado/PACKAGE_AUTOREMOTE_PREFIX; build/manifest sin cambios** — el cierre QA/Supervisor del 02/08/2026 VETÓ F1 y F3 del v1.1 (docs/DISENO-LOTE7-CIERRE-PUENTE.md v1.2, ADR-015 enmendado): **B1** (orden): `goAsync()` anula `mPendingResult` → `getSentFromUid()/getSentFromPackage()` llamados DESPUÉS de goAsync devuelven SIEMPRE -1/null (AOSP BroadcastReceiver.java, android15-release; el código implementado lo hacía: TaskerCommandReceiver.kt:58-59). **H1** (semántica de opt-in, invalida el diseño): la identidad del remitente solo se expone si el EMISOR hizo `BroadcastOptions.setShareIdentityEnabled(true)` — default FALSE (BroadcastRecord.java:489; BroadcastQueueModernImpl.java:1190-1220 pasa `r.shareIdentity ? r.callingUid : INVALID_UID`); doc oficial literal "if the sender opted in to sharing identity"; Tasker Send Intent / AutoRemote / adb shell NO activan el opt-in (no configurable) → la identidad del emisor es INOBTENIBLE con API pública en nuestro escenario, y los 57 tests del lote ejercitaban la capa pura con inputs que producción nunca produce (lección QA #7). **Rediseño v1.2**: F1 = TOKEN COMPARTIDO como extra `token` del TRANSPORTE (NO campo del contrato JSON: el codec decodifica con ignoreUnknownKeys=false y meterlo en el wire rompería la Fase 1 → CERO impacto en core/domain y en los 644 tests del contrato); fail-closed en el handler: `tokenCompartido` no-blank + extra ≠ token → `token_invalido` aditivo (6 claves, id eco, bridge NO ejecutado, respuesta emitida); blank = canal abierto (default, cero regresión, documentado). Se ELIMINAN `OrigenTasker`/`obtenerOrigenDe`/`TaskerOrigenVerifier` (+15 tests — falsa seguridad sobre un input imposible) y el seam vuelve a `TaskerMessageHandler.handle(extra, token: String?)`. F3 = flag explícito `enviarRespuestaURL` (default OFF) + key, `AutoRemoteUrlCallback.responderSiAplica(respuestaJson)` SIN origen (el startsWith com.bighugegiraffe muere con H1). F2 INTACTA. PuenteConfig v2 = {packageRespuesta, autoRemoteKey, tokenCompartido, enviarRespuestaURL} (−allowlistActivada/paquetesPermitidos/normalizarPaquetes); 4 providers; **45 tests del lote** (gate objetivo 740 CUMPLIDO (03/08/2026): BUILD SUCCESSFUL, 740 tests / 0 fallos (store 8, handler 22, extractor 22, emitter 9, urlBuilder 6, urlCallback 9, VM 8); Fase 1 (core/domain) SIN CAMBIOS; TaskerOrigenVerifierTest (15) ELIMINADO; cero referencias residuales a OrigenTasker/allowlist/origen_no_autorizado/PACKAGE_AUTOREMOTE_PREFIX; build/manifest sin cambios; era 752). **HISTORIAL v1.1 (implementado y GATE VERDE 02/08/2026)**: P0–P4 completos (7 suites del lote: PuenteConfigStoreTest 8, TaskerOrigenVerifierTest 15, TaskerMessageHandlerTest 19 [14 editados + 5 nuevos], TaskerResponseEmitterTest 9 [6 editados + 3 nuevos], AutoRemoteUrlBuilderTest 6, AutoRemoteUrlCallbackTest 10, PuenteSettingsViewModelTest 10 — 57 tests nuevos sobre los 695 previos) — `testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks` → **BUILD SUCCESSFUL, 752 tests / 0 fallos** (core:domain 435, service:system 225, core:data 29, feature:overlay 36, app 22, feature:chat 5). **ENMIENDA DE IMPLEMENTACIÓN (fuente real = android.jar de compileSdk 35, misma disciplina que las correcciones T4/T5)**: la premisa "`PendingResult.getSentUid()/getSentPackage()`, API 28" del diseño v1.1 era INCOMPILABLE — esos métodos no existen en la API pública de la SDK (son SystemApi/ocultos en AOSP; la verificación documental contra la rama android15-release no se sostiene en el classpath). Se usa la API pública equivalente `BroadcastReceiver.getSentFromUid()/getSentFromPackage()` (API 34+, Android 14; "initial sender", misma semántica de remitente real de ADR-015/P1) → guard pasa de API 28 a **API 34** (minSdk 26: API 26-33 → `OrigenTasker()` indeterminable → fail-closed con allowlist ON), la extracción se hace sobre el receiver (no el PendingResult) y la UI avisa "requiere Android 14+ (API 34)". Sin cambios estructurales: seam `OrigenTasker`, verifier, handler, tests y política F1/F2/F3 intactos (el input de tests M6 sigue siendo `OrigenTasker()` con uid/paquete null). Verificación O1 OK (sin imports fuera de bridge + core:data/util + app/ui/puente; AppModule = wiring del diseño). **PENDIENTE: validación manual en dispositivo** (guion del diseño: allowlist off/on con Tasker y `adb shell am broadcast` → `origen_no_autorizado`; Tasker Event recibe con setPackage default; URL callback con key; logcat uid 2000 denegado). **FIN DEL HISTORIAL v1.1**: el "PENDIENTE: validación manual en dispositivo" del v1.1 queda SIN EFECTO (el guion v1.2 de la sección 9 del diseño lo sustituye; la allowlist, el origen y el prefijo URL ya no existen). **PENDIENTE v1.2 (03/08/2026): validacion manual en dispositivo** - guion seccion 9 del diseno: humo canal abierto (blank), token erroneo/ausente - token_invalido (6 claves, id eco, bridge NO ejecutado), token correcto - ejecuta, flag OFF/ON URL callback, F2 setPackage taskerm. **Ronda UI/UX (02/08/2026, §13 del diseno): APROBADO CON CAMBIOS, todos aplicados** — switch clicable (Row toggleable + Role.Switch + Switch(onCheckedChange=null)), placeholder del paquete a stringResource, badge de estado del canal (Protegido/Canal abierto), AlertDialog de confirmacion de borrar key, captions a supportingText, labels/captions afinados; gate re-verificado tras los cambios: BUILD SUCCESSFUL, 740/0. CIERRE QA FINAL + SUPERVISOR (03/08/2026): **APROBADO** — rediseño v1.2a resuelve el veto de raiz (cero lecturas de identidad; token leido ANTES de goAsync; fail-closed testeado con coVerify exactly=0; nada loguea el token; F2/F0/timeout/eco/cancelacion intactos; hallazgos menores documentados en el diseno — fail-open del catch de cargar() §2.5, tests defensivos documentados, 2 tests duplicados benignos flag-on-sin-key, contradicciones documentales M1-M5 corregidas). Pendiente unico: validacion manual en dispositivo.
- **Lote 5 / Fase 1 — Puente Tasker (wire JSON)**: MOVIDO del backlog a "En curso" (01/08/2026). Implementación en marcha: registro de 22 wires, envelope polimórfico, codec 2 pasadas, puente que SIEMPRE ejecuta (ver ADR-013). Ronda de cierre (01/08/2026): fuente única de límites en AccionRegistry (rangos, teléfono, id/contexto ≤ 200), eco de id en los 4 data objects y NO-ALCANCE documentado (Fase 2/F5) ✅
- **Lote 6 / Fase 2 — Puente Tasker (transporte)**: DISEÑADO por el Arquitecto (02/08/2026, docs/DISENO-LOTE6-FASE2.md, ADR-014): broadcast como entrada (Tasker Send Intent primario + AutoRemote wrapper opcional sin SDK) y TTS + broadcast de vuelta como salida; receiver cáscara con patrón AlarmReceiver; seam puro TaskerMessageHandler; ~38 tests JVM sin dependencias nuevas. **v1.1 CERRADO (02/08/2026) tras shift-left de QA (H1-H11 — revisión documental/decisión, sin cambios estructurales): H1 adb con paquete posicional/-n, H2 riesgo real ACTION_CALL documentado, H3 silencio deliberado acotado, H4 QA #5 ACEPTADO, H5 passthrough 8192, H6 id eco en error_timeout, H7 riesgo TTS corregido, H8 import check → code review, H9 try/catch cáscara, H10 conteo corregido (8+4+4, ~38 tests), H11 guion ampliado**. ✅ **IMPLEMENTADO y GATE VERDE (02/08/2026)**: P0–P4 completos (8 producción + 4 tests nuevos + AppModule + manifest), P5 cerrado — `testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks` → **BUILD SUCCESSFUL, 689 tests / 0 fallos** (644 previos + 45 nuevos: TaskerPayloadExtractorTest 19, TaskerMessageHandlerTest 13, TaskerRespuestaTextoTest 7, TaskerResponseEmitterTest 6). Receiver validado manualmente en dispositivo (guion en el informe del lote: adb am broadcast con paquete posicional/-n, Send Intent + Event Broadcast Received, caso negativo sin extras → silencio; latencia cached state y quoting PowerShell — H11). **CIERRE QA/Supervisor (02/08/2026)**: buffer de TTS en TextToSpeechManager (BUFFER_MAX=5, drena en onInit), KDoc origen = TARGET (Fase 3 → getSentUid/getSentPackage API 28+), riesgo de privacidad del broadcast de respuesta documentado (T4), CancellationException re-lanzada en el handler, emitter test con lectura real del slot, KDoc timeout del receiver — **gate final: BUILD SUCCESSFUL, 695 tests / 0 fallos** (689 + TextToSpeechManagerTest 5 + 1 cancelación handler)

## Auditoría exhaustiva — 03/08/2026 (código completo, 4 frentes: Arquitecto + 2×QA + Supervisor; 134 archivos Kotlin, ~16.7k líneas)

Veredicto global: **SÓLIDO CON HALLAZGOS — cero bloqueantes; 7 bugs reales + deuda estructural + ~25 menores**. Frente candidato: **Lote 8 — Saneamiento** (bugs B1–B7 + menores baratos). Los hallazgos quedan registrados aquí para trazabilidad.

### BUGS REALES (corrección prioritaria — propuesta Lote 8)

| ID | Bug | Evidencia | Gravedad |
|----|-----|-----------|----------|
| **B1** | "pon **la** alarma a las 7" → `OpenAlarms` (guía de error) en vez de `SetAlarm(7,0)`: el anchor `(?<![a-z])(las\|la)\s+` con `.find()` captura el artículo "la " del sustantivo en la rama 12 (ADR-007 ya lo documentó para la rama 11b y lo resolvió con `substringAfter("alarma")` — NUNCA se aplicó a la rama 12); "la alarma de las 7" / "activa la alarma a las 7:30" igual; sin NINGÚN test con "la" | `TimePhraseParser.kt:59` + `SystemCommandParser.kt:319-327` — repro VERIFICADO por ejecución | **MAYOR** |
| **B2** | Function-call de Gemini sin validar: `{"hour":25}` → alarma a las 1:00 silenciosa (Calendar normaliza); `{"hour":-1}` → requestCode -60 → fila persistida en Room pero el receiver ignora requestCode<0 → **alarma fantasma que nunca suena**; el invariante HourMinute(0..23,0..59) se garantiza en voz y wire pero NO en este path; sin test | `GeminiRepository.kt:237-238` + `AlarmAction.kt:68` | **MAYOR** |
| **B3** | **Captura de pantalla desconectada en producción**: `ScreenContextRepositoryImpl.captureScreenshot()` devuelve `null` SIEMPRE (comentario "se delega en el servicio"); la maquinaria real (`ScreenContextService.takeScreenshot`, singleton estático) no tiene NINGÚN call site; el prompt "analiza mi pantalla" viaja a Gemini SIN imagen (solo texto de accesibilidad); O8 del backlog sigue pendiente; el seam `CaptureScreenContextUseCase` existe pero está roto | `core/data/repository/ScreenContextRepositoryImpl.kt:17-21` + `service/system/ScreenContextService.kt:36-72` (grep 0 usos) | **MAYOR** |
| **B4** | **"Quitar API key" no persiste**: `AppModule.kt:270-284` ejecuta `storeApiKey(buildConfigKey)` como efecto secundario del `@Provides` cada vez que se crea el singleton (key en blanco → se re-siembra desde secrets.properties); el diálogo promete "quedará sin IA" — FALSO tras reiniciar; además escritura en prefs cifradas (Keystore) dentro del grafo DI en el hilo del primer acceso | `app/.../di/AppModule.kt:270-284` + `ApiKeySection.kt:152-153` | **MAYOR** |
| **B5** | **CancellationException engullida en la capa de acciones**: `SystemActionHandler.kt:72` y 11 acciones (`catch (e: Exception)`) convierten la cancelación en error; con `withTimeoutOrNull(10s)` del handler, la acción sigue ejecutando IO no cancelable tras el deadline (amplía la ventana goAsync real); contraste: handler/receiver/callback del puente SÍ re-lanzan (patrón declarado en el propio módulo, con test) | `SystemActionHandler.kt:72`, `NoteAction.kt:72-73`, `SystemVolumeAction.kt:76`, `AppLauncherAction.kt:59`, `CallAction.kt:26/40/54`, `MapsAction.kt:23`, `TimerAction.kt:22`, `MediaAction.kt:20/37/60`, `MessagingAction.kt:28/55`, `SearchAction.kt:33`, `SettingsAction.kt:20` | **MAYOR** |
| **B6** | **TextToSpeechManager: carrera entre hilos**: `speak()` se invoca desde IO mientras `onInit` (main) ejecuta `drenarBuffer()` sobre el MISMO `ArrayDeque` (no thread-safe) → corrupción de índice en la ventana de arranque (justo lo que el buffer cubre); `isInitialized` var sin `@Volatile` → lectura stale puede encolar para siempre (respuestas mudas) | `TextToSpeechManager.kt:29-33,62-78` | **MAYOR** |
| **B7** | **Semántica del wire: `estado="ok"` con `"Error: ..."` en `resultado`**: las acciones devuelven strings ADR-009 y `SystemActionHandler` las envuelve SIEMPRE en `Success`; `estado="error"`+`fallo_ejecucion` solo para excepciones — un guion Tasker que filtre por `%estado=="ok"` trata fallos reales como éxito (contradice H4/S4 del ADR-013); TTS del puente habla el prefijo mientras el overlay lo quita (inconsistencia del canal humano); `TaskerBridgeTest` no cubre el caso; **requiere decisión de diseño** | `SystemActionHandler.kt:71` + `SystemCommandBridgeImpl.kt:49-50` | **MAYOR** |

### Deuda estructural (mayor — próximos lotes)

- **D1** — Inversión de límites: `service:system → feature:overlay/chat` (`service/system/build.gradle.kts:40-41`; `AssistantOverlayService` importa `ChatViewModel`/`AssistantOverlayUI`/`OverlayViewModel`; `NavGraph.kt` importa `ChatScreen`) — la única violación de capas del proyecto; el servicio ES parte de la feature overlay → mover `AssistantOverlayService` a `feature:overlay` (recomendado; requiere mover manifest)
- **D2** — Feature chat muerta: `NavGraph` sin call sites + `chatViewModel` lazy nunca consumido + `ChatScreen` sin punto de entrada; **`SendMessageUseCase` (core/domain) sin call sites** mientras su lógica está triplicada y divergente (OverlayViewModel hardcodea `isNetworkAvailable=true`, ChatViewModel sin red); `Message` vs `ChatMessage` (dos modelos); `ConversationRepository` inyectado y NUNCA usado (el historial persistido es callejón sin salida); ChatViewModel expone `e.localizedMessage` crudo al usuario, sin Job de cancelación ni guard isLoading → decidir: integrar o eliminar el módulo
- **D3** — Segundo catálogo de acciones: 8 `FunctionDeclaration`s de Gemini (`GeminiRepository.kt:40-117`) en paralelo a `AccionRegistry` (22 wires) con vocabulario distinto (`open_alarms` vs `abrir_alarmas`) → añadir una acción = tocar parser + registro + handler + tabla de tools
- **D4** — Patrón cifrado+fallback duplicado: `ApiKeyProvider` vs `PuenteConfigStore` (bloque casi 1:1) → extraer base `EncryptedPrefsStore` (2 copias hoy; el 3er store lo hará obligatorio)
- **D5** — `compileOnly` frágil de serialization-json en `:app` (runtime llega solo por transitividad de service:system; si cambia → NoClassDefFoundError silencioso) → `implementation` (1 línea)
- **D6** — `provideMessageQueueManager` sin `@Singleton` aunque la clase lo declara → provider sin scope crea instancias nuevas; corregir o quitar la anotación
- **D7** — `ScreenContextService` service-locator estático (WeakReference + callback único: dos `takeScreenshot` solapados se pisan) → para O8, migrar a inyección vía `ScreenContextRepository`

### Menores (backlog técnico)

- **M1** — Voz vs wire: parser sin límites en el temporizador ("temporizador de cero minutos" → `SetTimer(0)`; "de 5000" → pasa) vs wire 1..1440; el contrato `SetTimer` no declara su invariante — ✅ **PAGADO (Lote 10)**: invariante `[TIMER_MIN_MINUTOS, TIMER_MAX_MINUTOS]` en SystemCommand (fuente única, AccionRegistry la consulta); parser valida antes de ejecutar + guard en TimerAction; +8 tests (incl. boundaries 1/1440 y P2-1 no-numérico)
- **M2** — "llama a 600 123 456 **por favor**" → `Call("600 123 456 por favor")` (el cortesía rompe `matches()` del phoneRegex → cae a contacto basura); "llama a la oficina" debe seguir → Call(contacto) — ✅ **PAGADO (Lote 10)**: strip del sufijo de cortesía (`por favor`/`porfavor`/`porfa`, case-insensitive) en callOrNumber + guard "sin target" → error SIN ejecutar; +5 tests (incl. variante porfavor P1-1); regresión "llama al jefe"/"llama a 600 123 456" verde sin tocar
- **M3** — `streamMessage` (GeminiRepository.kt:336-341): try/catch solo envuelve la construcción del Flow; IOException en colección se propaga sin transformar + "Error en streaming: ..." filtra internals del SDK (latente hoy: sin consumidor)
- **M4** — `CorrespondenciaWireTest` no detecta ADICIONES de wires (itera tabla fija; añadir wire 23 sin tocar el test → verde) → `assertEquals(AccionRegistry.todosLosWires, tabla.map{it.first})`
- **M5** — `restoreActiveAlarms` sin aislamiento por fila: si `setExact` lanza en una fila, `runCatching` del arranque traga y el resto NO se reprograma (restauración parcial silenciosa)
- **M6** — Sin tests de migración Room (MIGRATION_2_3 ni @Upsert/deleteAll) — solo validación en dispositivo; sugerencia: MigrationTestHelper + inMemoryDatabase — ⏸️ **DIFERIDO (Lote 10)**: room-testing exige instrumentación/Robolectric (regla del equipo sin Robolectric ni deps); riesgo bajo (CREATE TABLE IF NOT EXISTS idempotente + fallbackToDestructiveMigration); ACLARACIÓN: MessageDao usa @Insert/@Query — la sugerencia @Upsert/deleteAll no refleja el DAO real. Reabrir con M13 (infra de instrumentación) — ✅ **PAGADO (Lote 12)**: ver cierre del lote (migración 2→3 con SQLite real + CRUD de DAOs, JVM + Robolectric, excepción de regla autorizada)
- **M7** — `HttpUrlSender`: `disconnect()` solo en camino feliz (fuga de socket si responseCode lanza) + `Log.w(e.message)` puede filtrar la URL con la key de AutoRemote en MalformedURLException → finally + log seguro
- **M8** — `ScreenContextService`: `recycle()` deprecado API 33+ (guard SDK_INT<33); hardwareBuffer/bitmaps sin cerrar/reciclar tras `copy()` (fuga nativa por captura); StringBuilder de texto de pantalla sin límite
- **M9** — `MediaAction.openYouTube`: query sin `Uri.encode` (espacios/`&` → URIs malformados) + fallback que relanza `startActivity` fuera de try; `SearchAction` usa columna `MediaStore.DATA` deprecada (null en API 29+ → "en null")
- **M10** — `SystemActionHandler.kt:73`: `ActionResult.Error(e.message ?: ...)` pasa el mensaje bruto al usuario (espíritu ADR-009/O7; hoy solo MemoryAction puede lanzar — el fix B5 lo cubre) — ✅ **PAGADO (Lote 10)**: catch externo sanea el mensaje (Log.w + "No pudo completarse la acción."); 3 tests editados + P2-3 (SQL interno no se filtra); B5 intacto
- **M11** — `AudioManager.ADJUST_MUTE` deprecado API 31+ → `ADJUST_TOGGLE_MUTE`
- **M12** — Strings: ~25+ hardcodes en 4 pantallas (MainActivity 7, ChatScreen 5, AssistantOverlayUI 7, VMs) + 18 strings muertas en strings.xml (start_assistant, message_hint, error_connection, notification_title/text...) → migrar o eliminar (regla UI del gate)
- **M13** — Gate no mecanizado: sin JaCoCo/verificación de cobertura en ningún build.gradle.kts (el "gate" es comando manual + conteo en BACKLOG) + `app/src/androidTest/` vacío (cero tests Compose) → mecanizar (JaCoCo + mínimo) y arrancar UI tests de MainScreen/ApiKeySection — ⏸️ **DIFERIDO (Lote 10)**: JaCoCo multi-módulo con AGP 8.x = infra de build de riesgo medio-alto sin pipeline que ejecute androidTest; el gate JVM sigue siendo reproducible (comando documentado + conteo por XML) — ✅ **PAGADO (Lote 12)**: gate mecanizado `gradlew gate --rerun-tasks` (JaCoCo 0.8.13 por módulo + tarea raíz), androidTest mínimo de UI (ver cierre del lote)
- **M14** — Cadena de permisos rota en MainActivity: callback de POST_NOTIFICATIONS vacío + `return` tras lanzarlo (el usuario concede y debe pulsar de nuevo); SYSTEM_ALERT_WINDOW no se re-chequea al volver de Settings → reanudar cadena en callback/onResume
- **M15** — OverlayViewModel: `conversationRepository`/`screenContextRepository` inyectados y sin uso; `isNetworkAvailable = true` muerta; `OverlayUiState.messages`/`currentOutfitRes` nunca escritos — ✅ **PAGADO (Lote 10)**: conversationRepository (D2, Lote 9) + screenContextRepository fuera del VM/servicio (el use case lo lleva), isNetworkAvailable eliminada, OverlayUiState.currentOutfitRes eliminado (CharacterState.currentOutfitRes se mantiene con su test); OverlayViewModelTest sin el mock (24 tests intactos)
- **M16** — `changeOutfit` (`currentOutfitIndex + 1`) crece sin límite vs `CharacterState.nextOutfit()` módulo 10 → IndexOutOfBounds latente si se indexa la library desde el uiState
- **M17** — Drag del overlay sin clamp (x/y acumulativos → el personaje puede perderse fuera de pantalla)
- **M18** — Fallos silenciosos de persistencia: `PuenteConfigStore.guardar`/`ApiKeyProvider.storeApiKey` tragan excepciones; VMs marcan "guardado" sin saber si persistió (aceptado por diseño v1.2a — documentar en KDoc) — ✅ **PAGADO (Lote 10)**: contrato fail-soft v1.2a declarado en el KDoc de ambos puntos de entrada (la base ya lo documentaba en EncryptedPrefsStore)
- **M19** — TTS con `Locale.getDefault()` (no fuerza español): en dispositivo en otro idioma el asistente habla en ese idioma
- **M20** — `_viewModelStore.clear()` en AssistantOverlayService es inefectivo (VMs construidos manualmente, nunca registrados) — falsa garantía de onCleared; funciona solo por DisposableEffect — ✅ **PAGADO (Lote 10)**: garantía documental CORREGIDA en el KDoc del servicio (no-op formal; limpieza real = DisposableEffect de la UI; store se mantiene por setViewTreeViewModelStoreOwner); NO registrar los VMs (veto del Arquitecto: doble destroy TTS/STT)
- **M21** — Captura de contexto ANTES de parsear comandos directos (OverlayViewModel.kt:60-65, intencional test 4): cuando la captura funcione (B3), cada "pon una alarma" pagará latencia de captura innecesaria → capturar solo si parse devuelve null
- **M22** — `TIMEOUT_MS = 10_000L` duplicada (TaskerBridgeContract:25 y AlarmReceiver:45); `NetworkUtils` muerto (SystemActionHandler reimplementa hasNetwork inline); `ConnectivityWorker` stub programado cada hora (work que consume batería sin efecto — decidir completar o desprogramar) — ✅ **PAGADO (Lote 10)**: TIMEOUT_MS ÚNICA en TaskerBridgeContract; NetworkUtils.kt ELIMINADO (cero call sites; ETHERNET no se añade a hasNetwork — diferencia deliberada documentada en su KDoc); ConnectivityWorker zombie ya cancelado (Lote 8, cancelConnectivityWorkerZombie)
- **M23** — Premisa documental falsa en ADR-003/NoteAction.kt:112-114: "core:domain no debe filtrarse a service:system" — FALSO (service:system SÍ depende de core:domain); la duplicación de normalizeLocal SÍ está justificada (contrato distinto: colapso de espacios) pero la justificación citada es incorrecta → corregir KDoc/ADR; evaluar umbral del 3er consumidor — ✅ **PAGADO (Lote 10)**: KDoc de NoteAction corregido (razón = contrato DISTINTO, no capas) + ADR-003 enmendado en este BACKLOG; umbral del 3er consumidor NO alcanzado (hoy 2) → documentado, sin refactor
- **M24** — Naming: impls con mismo nombre que interfaces (`MemoryRepository.kt`/`GeminiRepository.kt` vs `core/domain/repository/`) → `*Impl` (precedente interno); convención DI: DataModule usa @Binds, AppModule @Provides con FQCN (4 repos candidatos a @Binds); versión de Room duplicada (2.6.1 en app y core/data) → version catalog — ✅ **PAGADO (Lote 10)**: renames `*Impl` (git mv), 4 @Binds en DataModule (3 @Provides FQCN de AppModule ELIMINADOS), `gradle/libs.versions.toml` creado (mínimo: room/junit/mockk/coroutines-test; Room sigue 2.6.1, sin bump)
- **M25** — `NoteAction.kt:70-74`: catch(IOException) seguido de catch(Exception) redundante; `AlarmAction.kt:99-100` fallback `hour ?: 0` inalcanzable (parser/codec rechazan medio-null) — código defensivo muerto; `ConnectivityWorker` imports Assisted/AssistedInject sin uso — ✅ **CERRADO (Lote 10)**: catch colapsado + imports muertos (Lote 8, verificado); `hour ?: 0` MANTENIDO como red de contrato de API cubierto por test (AlarmActionTest:523) con KDoc actualizado — NO eliminar (veto); catch de NoteAction anotado en KDoc — NO eliminar (veto)
- **M26** — Tests: casos de voz reales sin cobertura — "a las 7 y media" suelto, "temporizador de 5" sin unidad, "las 7" suelto (decidir y fijar comportamiento); aserción débil `coEvery(any())` en SystemCommandParserTest.kt:83-89; fakes manuales en ChatViewModelTest (regla 6 del equipo dice MockK) — ✅ **CERRADO (Lote 10, verificado 0 trabajo)**: los 3 casos de voz YA fijados con tests (L99-125); stub débil ya tipado (L84-85); fakes eliminados con feature:chat (D2, Lote 9)

### Verificaciones que pasaron (sin defectos — confirmado por 4 frentes)

- core:domain JVM puro (0 imports android.*); frontera protocolo/transporte del puente respetada; biyección wire 22/22 custodiada; precedencia de errores E4-E8 en todos los paths; B3 estricto verificado por ejecución
- DI: scopes, Lazy anti-ciclo LanguageAction, IoDispatcher, sin dobles bindings; TOCTOU setExact→upsert; fábrica única de PendingIntent; guard de permisos en restore
- Puente: B1 (extras antes de goAsync), H9 (codec inyectado), token fail-closed con trim/case-sensitive, H3 (respuesta siempre emitida) — todo conforme
- Tests sin falsos positivos relevantes (patrón coEvery+coVerify+exactly=0); puente/acciones/parser con cobertura real

---

## P1 — Siguiente sprint (impacto alto, esfuerzo bajo/medio)

| ID | Mejora | Detalle | Esfuerzo |
|----|--------|---------|----------|
| *(vacío — P1 resuelto el 31/07/2026: O3, O4, H4 ✅)* | | | |

## P2 — Parsing y consistencia (impacto medio)

| ID | Mejora | Detalle | Esfuerzo |
|----|--------|---------|----------|
| *(vacío — P2 resuelto el 01/08/2026: TMP-2, TMP-num, MIN-dígito, O6, H3, H5 ✅)* | | | |

## P3 — Notas (feature crear y leer notas ya entregada)

| ID | Mejora | Detalle | Esfuerzo |
|----|--------|---------|----------|
| N3 | Títulos, edición y borrado de notas | Metadatos diferidos; hoy un archivo = una nota (solo texto) | Medio |
| N2 | Abrir notas con FileProvider / notificación | ⏸️ DIFERIDO (Lote 4): diseño de referencia pendiente; requiere manifest + `file_paths.xml` + decisión de UX — sin pantalla propia de notas no compensa. Revisar con producto | Medio |

## P4 — Producto / UX

| ID | Mejora | Detalle | Esfuerzo |
|----|--------|---------|----------|
| O8 | Captura de pantalla en comandos directos | "¿qué hay en mi pantalla?" sin pasar por la ruta Gemini estándar; integrar análisis de pantalla | Medio |
| MULTI | Navegación multi-paso por voz | "primero X, luego Y" | Medio |
| FEED | Sonidos/feedback de confirmación para acciones críticas | Llamadas, SMS | Bajo |

## Resuelto recientemente (referencia)

- **O3** — "las doce de la noche" → 0:00 (medianoche) y "las doce de la manana" → 12:00 (mediodía): caso especial hour==12 en el período (cambio semántico aprobado: antes manana daba 0:00) ✅
- **O4 (P1)** — "cancela la alarma de las 7" → CancelAlarm(hora?, minuto?) con precedencia sobre la rama 12 (antes ejecutaba SetAlarm): en P1 abría ACTION_SHOW_ALARMS con guía TTS; superado por O4-P2 (cancelación real, ver ADR-008) ✅
- **O4-P2** — cancelación REAL de alarmas: migración completa a AlarmManager in-app (se abandona ACTION_SET_ALARM/ACTION_SHOW_ALARMS). AlarmScheduler+AndroidAlarmScheduler, fábrica única de PendingIntent (requestCode=hora*60+min, action ALARM_FIRED, setPackage, FLAG_UPDATE_CURRENT|IMMUTABLE), persistencia Room v3 (AlarmEntity/AlarmDao/ActiveAlarmStore + MIGRATION_2_3 explícita), AlarmNotificationHelper (canal alarm_channel IMPORTANCE_HIGH, contentIntent a MainActivity), AlarmReceiver (@AndroidEntryPoint, goAsync+timeout 10s), restore en app onCreate (self-healing; tras reboot hay que abrir la app una vez), SCHEDULE_EXACT_ALARM (no USE_EXACT_ALARM, política targetSdk 35), label "para X" conectada hasta la notificación. "cancela la alarma" sin hora → cancela TODAS (cumple la intención original del parser). Guard de permiso también en restore (evita alarma fantasma silenciosa) ✅
- **O7** — convención única "Éxito: "/"Error: " en TODOS los mensajes de acción (parser: "ERROR:"→"Error:", overlay: removePrefix encadenado, 8 acciones + AlarmAction normalizadas string a string, errores sin excepciones crudas: "Error: No se pudo buscar archivos."). HELP_SPEECH/fallbacks quedan sin prefijo (removePrefix no los toca) ✅
- **H4** — "llama al 600.123.456" → CallNumber("600123456"): '.' añadido a phoneRegex y al strip de sanitización ✅
- **O5** — duración compuesta: "1 hora y 30 minutos" → 90 min (rama 3: `findAll` + suma de unidades + fracción "y media/cuarto") ✅
- **TMP-2 (P2)** — "temporizador de 1 hora y 30" sin unidad → 90 min: dígito desnudo tras "X horas/minutos y N" = SIEMPRE minutos 0..59, lookahead anti-doble-conteo y trailing sin unidad ignorado ("1 hora y 30 y 5"→90); >59 → Gemini ("1 hora y 60") ✅ (ver ADR-TMP-5)
- **TMP-num (P2)** — palabras numéricas de duración: "una hora"→60, "una hora y media"→90, "media hora"→30, "un cuarto de hora"→15, "un minuto"→1, "veinte y cinco minutos"→25; segundos → ceil a 1 minuto ("45 segundos"→1, "cuarenta y cinco segundos"→1); "X minutos y medio" residual → Gemini; >59 hablado sin decena soportada ("sesenta y cinco") → Gemini; "pasa una hora" aceptado como extensión del falso positivo ✅ (ver ADR-TMP-6)
- **MIN-dígito (P2)** — "las siete y veinte y 5" → 7:25: token2 del compuesto acepta dígito 1-9 con la MISMA validación que las palabras ("y 10" y "y media y 5" → null, B3) ✅ (ver ADR-TMP-7)
- **O6 (P2)** — espacios iniciales reales: `parse()` hace `normalize(text).trim()` (trim COMPLETO, no solo lowercase); normalize y UI intactas; extractAfterPrefix recibe trimmed y original.trim() ambos trimados → alineación 1:1 restaurada (14 tests con 1-2 espacios iniciales en todas las ramas) ✅ (ver ADR-010)
- **H3 (P2)** — "busca: gatos" / "repite: eso": el ':' tras PALABRA es separador de comando (variantes `busca:`/`buscar:` en rama 10 con limpieza `.trim().trimStart(':').trim()` que además arregla el bug preexistente "busca : gatos"; `repite:` en rama 2 sin extracción; el ':' tras DÍGITO sigue siendo hora en la rama 12) ✅ (ver ADR-011)
- **H5 (P2)** — "que puedes hacer:" con ':' → HELP: lookahead `(?=\s|:|$)`; falso positivo aceptado "piensa que puedes hacer: X" (patrón marginal por voz) ✅ (ver ADR-011)
- **O9** — minutos hablados completos 0-59 ("y cero"→7:00, "y treinta y cinco"→7:35, "menos veinte"→2:40) + compuestos "decena y unidad 1-9" ✅
- **N-OBS1** — contracción "del": "toma nota del pan" → "pan" (prefijos " del " + colgante "del" → fall-through) ✅
- **AYUDA** — "¿En qué me puedo ayudar?" → respuesta hablada con la lista de capacidades (HELP_PHRASES + normalize de signos, ':' excluido) ✅
- **N-OBS2 (P3)** — "crea una nota de: pan" → CreateNote("pan"): variantes "de:" añadidas a los 3 grupos con preposición ("crea/crear", "escribe/escribir", "toma/tomar nota"); "de:" ANTES del " " desnudo del grupo (si no, la nota sería "de: pan"); "anota" y "apunta" sin tocar; "crea una nota del: pan" → Gemini (no-goal) ✅
- **N-OBS3 (P3)** — invariante de prefijos de notas documentada como regla (todo prefijo termina en espacio o ':'); aplicada también a la rama de lectura (N1); colgantes "de"/"del" filtrados por guard ✅ (ver ADR-004 enmendado)
- **N-OBS4 (P3)** — test del catch de `writeText`: 3er parámetro inyectable `writeNote: (File, String) -> Unit` (default = writeText UTF-8; AppModule posicional intacto); test con lambda que lanza IOException("disco lleno") sobre notesDir REAL + captura de (dir, "1750000000000.txt") ✅
- **N-OBS5 (P3)** — emoji en el límite: `truncateByCodePoints` (avanza por code points vía `Character.charCount`/`codePointAt`, nunca parte un surrogate pair) aplicado a take(1000) del guardado, EXCERPT_LENGTH y READ_CHARS_LIMIT; el aviso refleja el corte real (`finalText.length < text.length`, no la comparación previa) — 999a+😀+20b → archivo de 1001 UTF-16 terminando en 😀 completo ✅ (ver ADR-002 enmendado)
- **N1 (P3)** — lectura de notas por voz: "lee mis notas" → resumen (0 → "Error: No tienes notas."; 1 → singular; N → plural "La más reciente empieza así: «{excerpt}»"; extracto 80 + "..."); "lee la nota de X" → búsqueda por contenido normalizado (0 → error con query; 1 → completa si ≤ 400 chars, si no "es larga, {len} caracteres; te leo el principio"; N → más reciente con aviso de coincidencias). Rama 0b con precedencia sobre ramas 3/12; igualdad EXACTA en "lee mis notas" (bloqueante: "lee mis notas de la semana" → Gemini); archivos ajenos al formato `^\d{13}\.txt$` ignorados ✅ (ver ADR-012)

## Decisiones registradas (ADRs ligeros, sin archivo propio)

- **ADR-013 (código)**: puente Tasker — wire JSON único y biyección con el modelo de voz:
  - **H1 (wire único)**: 22 wires snake_case en español, uno por subtipo de SystemCommand, biyección EXACTA case-sensitive wire↔envelope↔SystemCommand en una ÚNICA fuente de verdad (AccionRegistry, que además guarda tipo y límites). Tabla: `llamar_contacto`, `enviar_sms`, `poner_alarma`, `cancelar_alarma`, `abrir_app`, `buscar_archivo`, `encolar_mensaje`, `abrir_alarmas`, `buscar_google`, `abrir_youtube`, `abrir_whatsapp`, `reproducir_musica`, `poner_volumen`, `poner_idioma`, `poner_temporizador`, `navegar_a`, `abrir_ajustes`, `llamar_numero`, `recordar_dato`, `crear_nota`, `leer_notas`, `leer_nota`. Justificación: `poner_alarma` (no `set_alarm` — el wire es NUESTRO vocabulario, no el del sistema) y `navegar_a` con preposición (el destino es argumento; "navegar" desnudo no es natural en español)
  - **H2 (build)**: plugin kotlinx-serialization 2.0.0 aplicado a core/domain (JVM puro sigue sin Android); la dependencia `kotlinx-serialization-json:1.7.3` ya estaba en core/data (intocada)
  - **H3 (codec 2 pasadas)**: decode en F1 (estructura: parseo→json_invalido; raíz no-objeto/version ausente-no-Int-≠1→version_no_soportada; accion ausente-no-String-no registrada→accion_desconocida) y F2 (tipos vía `decodeFromJsonElement<CommandEnvelope>`→SerializationException→json_invalido; los 4 data objects se decodifican A MANO: claves extra → json_invalido e id/contexto no-String → json_invalido, asimetría con las data classes), seguida de la Fase B' (semántica post-decode por campo en orden de declaración: primero longitud, luego rango/valores, precedida de los límites de la base id/contexto ≤ 200). Precedencia de errores E4>E5>E6>E7>E8 — leyenda: E4=entrada blank→json_invalido; E5=comando > 8192 chars→longitud_excedida; E6=parseo fallido→json_invalido y raíz no-objeto→version_no_soportada; E7=version ausente/no Int/≠1→version_no_soportada; E8=accion ausente/no String/no registrada→accion_desconocida. (F0 gana a todo; F2 y Fase B' corren después de F1.) El codec NUNCA ejecuta
  - **H4 (respuesta fija)**: esquema SIEMPRE de 6 claves `{version,id,estado,resultado,error,mensaje}` con `explicitNulls=true` → `"resultado":null` literal en errores. Envelopes exactos: éxito `{"version":1,"id":"t-42","estado":"ok","resultado":"Éxito: ...","error":null,"mensaje":null}`; json_invalido/longitud_excedida/version_no_soportada/accion_desconocida con sus mensajes "Error: ..." fijos; `accion_desconocida` con wire si existe (`'hacer_magia'`) o fijo si no
  - **H5 (base sealed)**: `CommandEnvelope` sealed con `@JsonClassDiscriminator("accion")` y 3 abstract vals (`version` default 1, `id`, `contexto`) overrideados por los 22 subtipos (18 data class + 4 data object); `poner_volumen` usa el campo `valor` (no `accion`) para no colisionar con el discriminante. Fase B' valida también tablas (volumen/idioma/plataforma) y teléfono `^\+?\d{3,15}$`
  - **H6 (tests)**: JUnit 4 (mismo marco que el resto del módulo; sin kotlin-test); MockK 1.13.12 y coroutines-test 1.9.0 ya presentes
  - **S1 (asimetrías voz↔puente)**: el puente es MÁS estricto que la voz (Tasker puede mandar basura): SetTimer sin tope por voz→puente 1..1440; CallNumber sin rango por voz→puente 3..15 dígitos. Asimetría aceptada por diseño
  - **S2 (rangos alarma)**: hora 0..23, minuto 0..59; cancelar_alarma exige hora y minuto JUNTOS o NINGUNO (`7,null` y `null,30` → valor_invalido con mensaje propio)
  - **S3 (enums sin anotar)**: VolumeAction/AssistantLanguage NO son @Serializable (intocados); el wire viaja en string y las tablas wire→enum viven en el codec (subir→UP, bajar→DOWN, maximo→MAX, minimo→MIN, silencio→MUTE; espanol→SPANISH, ingles→ENGLISH)
  - **S4 (fallo_ejecucion)**: el puente SIEMPRE ejecuta (Fase 1); ActionResult.Error y excepción de execute → código `fallo_ejecucion` con reason VERBATIM `"Error: " + reason` (patrón SystemCommandParser.execute:407 / ADR-009)
  - **S5 (Json inyectable)**: `SystemCommandJsonCodec(json: Json = Json { ignoreUnknownKeys = false; explicitNulls = true })` — inyectable para tests sin tocar defaults; `Json` default documenta el contrato
  - **S6 (límite 8192)**: longitud de comando en CHARS (no bytes); F0: blank→json_invalido, >8192→longitud_excedida (`comando (maximo 8192)`)
  - **S7 (fuente única de límites)**: la Fase B' consulta TODOS sus límites en AccionRegistry, nada se hardcodea en el codec — longitudes por campo, rangos (hora 0..23 / minuto 0..59 / minutos 1..1440 vía `Limites.rangoDe(campo)`), tablas de valores, teléfono (constantes TELEFONO_MIN_DIGITOS=3 / TELEFONO_MAX_DIGITOS=15, regex construida desde ellas, longitud máx. 16 = '+' + 15 dígitos) y la base del envelope: id/contexto ≤ 200 CHARS (MAX_ID_CONTEXTO_CHARS), validados sobre el wire (cubre los 4 data objects cuyo envelope no conserva id/contexto) con el id eco en el mensaje. Guardián: `CorrespondenciaWireTest` verifica registro==codec (rangos, teléfono e id/contexto)
  - **NO-ALCANCE (Fase 1, ENMENDADO por ADR-014 y ADR-015)**: la Fase 2 del puente (transportes para el wire JSON y respuesta al emisor) queda FUERA de la Fase 1 y se resuelve en el Lote 6 (ver ADR-014: broadcast de entrada + TTS/broadcast de salida). La Fase 3 (allowlist de emisores + setPackage de respuesta + URL callback de AutoRemote) se resuelve en el Lote 7 (ver ADR-015) — ya NO está fuera de alcance. Sigue FUERA: la F5 (confirmación humana de acciones irreversibles — el control pendiente que cierra H2 estructuralmente; el puente ejecuta llamadas/SMS sin validación humana del wire).
- **ADR-014 (código)**: puente Tasker — Fase 2, transporte: broadcast como entrada (Tasker Send Intent, canal primario; AutoRemote como wrapper opcional de coste ~0) y TTS + broadcast de vuelta como salida; receiver cáscara con patrón AlarmReceiver; seam puro `TaskerMessageHandler` (ver docs/DISENO-LOTE6-FASE2.md):
  - **T1 (mecanismo)**: (b) Send Intent nativo de Tasker (action única NUESTRA `com.screenassistant.TASKER_COMMAND`, extra `cmd`) como canal primario; (c) AutoRemote aceptado SIN dependencia/SDK (extra canónico `message`, action de re-emisión configurable a la nuestra, target package). Se descarta (a) puro: app extra + key por dispositivo + escritorio solo para la respuesta; ecosistema = 1 dispositivo. Android 8+ (minSdk 26): broadcasts implícitos no llegan a receivers estáticos → el guion rellena el campo Package de Send Intent (package-specific, sí entregado); smoke QA por adb: paquete POSICIONAL final (`--es cmd '<json>' com.screenassistant`) o componente explícito (`-n com.screenassistant/.service.system.bridge.TaskerCommandReceiver`) — H1
  - **T2 (receiver)**: service/system (mismo módulo que bridge/acciones), registrado en app manifest exported=true + intent-filter (obligatorio targetSdk 35); sin directBootAware (necesita usuario desbloqueado: Room/filesDir/TTS). goAsync + withTimeoutOrNull(10s) + finish en finally (patrón AlarmReceiver); sin START_FOREGROUND: acciones actuales < ~2 s; timeout → error estructurado; criterio de migración: acción > ~8 s medidos → FGS specialUse con el MISMO handler (YAGNI, no se anticipa)
  - **T3 (payload)**: extracción de DECISIÓN pura en service/system (`TaskerPayloadExtractor`, JVM sin android.util — el vocabulario message/cmd es de transporte, no de protocolo; core/domain no se filtra, precedente ADR-003). Precedencia message (AutoRemote) > cmd; message presente vacío GANA (→ F0 json_invalido estructurado, mejor que silencio); sin extra → SILENCIO (return, patrón AlarmReceiver requestCode<0 — broadcasts basura no merecen TTS). Límite 8192 NO se trunca en transporte (fuente única en el puente, ADR-013 S6/S7). Convención nueva: `"contexto":"silencioso"` en el wire suprime TTS (parseo best-effort, sin falsos positivos) pero no el broadcast — el campo contexto del contrato gana semántica sin tocar core/domain
  - **T4 (respuesta)**: mínimo viable (a)+(b): TTS (canal humano; binding Hilt NUEVO TextToSpeech→TextToSpeechManager; coexisten 2 instancias TTS con el overlay, cada una con su PROPIO motor → riesgo real: habla SIMULTÁNEA de dos motores si coinciden, no colisión de cola — H7; uso secuencial aceptado) + broadcast `com.screenassistant.TASKER_RESPONSE` con extra `respuesta` (MISMO JSON de 6 claves, contrato H4 intacto, sin envoltorio) e `id` plano (filtro en Tasker por variable). (c) AutoRemote URL callback FUERA en Fase 2 (key + UI Settings + red; extensión Fase 3 con el mismo cuerpo JSON). **RIESGO DE PRIVACIDAD (cierre QA/Supervisor)**: el broadcast de vuelta es GLOBAL (sin permiso ni setPackage) → cualquier app del dispositivo puede recibir el extra `respuesta` con el JSON íntegro (`leer_nota`/`leer_notas` exponen el texto de la nota en `resultado`); control v1 = riesgo documentado (KDoc del emitter). **CORRECCIÓN (ADR-015, Lote 7)**: la afirmación "opciones Fase 3: setPackage configurable (rompe AutoRemote, que re-emite con su propio targetPackage) o permiso signature" era FALSA en su premisa — AutoRemote NO consume TASKER_RESPONSE (solo re-emite la ENTRADA; su salida es la URL callback); `setPackage` filtra por el package del RECEPTOR y Tasker Event "Broadcast Received" (registro dinámico bajo su propio package) SÍ recibe broadcasts con `setPackage(taskerm)`. El riesgo de privacidad queda CERRADO por defecto en Fase 3A con `packageRespuesta = net.dinglisch.android.taskerm` configurable (ADR-015/P3)
  - **T5 (seguridad)**: exported + intent-filter, SIN permiso custom (signature inviable: el emisor no comparte firma; normal inútil) y SIN validación de origen v1 (broadcasts no ordenados: getPackage() devuelve el TARGET si Tasker rellena Package). RIESGO MÁXIMO REAL (v1.1, H2): con CALL_PHONE concedido, ACTION_CALL marca DIRECTO sin diálogo (CallAction.kt:18,34) → una app maliciosa podría disparar llamadas automáticas; igual con SEND_SMS. Mitigación real: permisos concedidos explícitamente en Settings (la app maliciosa ya los tiene — el receiver no amplía la superficie, solo la hace programable por broadcast) + F5 (confirmación humana) como control pendiente que cierra el hueco; riesgo documentado; seam preparado (`origen` best-effort viaja al handler) → allowlist en Settings (Fase 3) sin cambiar interfaces. QA #5 ACEPTADO (H4): handler testeable con cualquier origen, riesgo máximo documentado. **CORRECCIÓN (cierre QA/Supervisor)**: con el guion OBLIGANDO el campo Package, `origen` = `intent.getPackage()` devuelve el TARGET (com.screenassistant), NO el emisor real; sin Package, `getPackage()` es null y el broadcast implícito no llega al receiver estático en API 26+ (la afirmación previa de `net.dinglisch.android.taskerm` era FALSA). **CORRECCIÓN 2 (ADR-015, Lote 7)**: la afirmación "getSentPackage solo en ordered → no fiable" era FALSA — `PendingResult.getSentUid()`/`getSentPackage()` (API 28+) funcionan en CUALQUIER broadcast (ordenado o no) y devuelven el REMITENTE real (Tasker = net.dinglisch.android.taskerm; AutoRemote = com.bighugegiraffe.*; adb shell = uid 2000/paquete null; sistema = uid 1000; -1/null si no determinable); validados en dispositivo (guion del Lote 7). La allowlist se implementa en el Lote 7 con origen REAL (OrigenTasker) — el seam SÍ cambia de interfaz (enmienda T6), deliberado y anunciado en el KDoc
  - **T6 (seam)**: `TaskerMessageHandler { suspend fun handle(extra: String, origen: String?): String }` — SIEMPRE JSON 6 claves (timeout incluido: withTimeoutOrNull → código NUEVO aditivo `error_timeout`, no rompe Fase 1, con id eco best-effort del wire — H6; try/catch defensivo → fallo_ejecucion, invariante "nunca excepciones hacia Tasker"). INVARIANTE ACOTADO (H3): "el emisor nunca espera sin respuesta" aplica desde el handler hacia arriba; extra ausente → silencio deliberado (anti-spam, precedente requestCode<0 de AlarmReceiver) es la única excepción. `TaskerResponseEmitter { fun emitir(json: String, hablar: Boolean) }` (parseo puro en TaskerRespuestaTexto: idDe/textoDe best-effort). Receiver cáscara ~20 líneas (patrón AlarmReceiver: no testeable sin Robolectric → cobertura en capa pura, política de "Notas de proceso") + try/catch defensivo en la cáscara: si el handler viola su contrato → `fallo_ejecucion` con codec inyectado (H9; finally{finish()} garantizado). **ENMIENDA (ADR-015, Lote 7)**: la Fase 3 cambia el seam DELIBERADAMENTE (anunciado en el KDoc) a `handle(extra: String, origen: OrigenTasker)` con origen REAL (uid/paquete del PendingResult, API 28+); QA #5 pasa de "sin coste" a "coste pequeño y declarado" (~4 tests del handler adaptados — CORREGIDO M2: son 14 tests existentes y TODOS cambian de constructor; solo 3 pasaban origen String no-null). `TaskerResponseEmitter` NO cambia de firma (el setPackage de F2 viene de la config compartida)
  - **T7 (tests)**: ~38 tests JVM (JUnit 4 + MockK + coroutines-test; SIN Robolectric ni dependencias nuevas): extractor ~15 (precedencia/silencio/silencioso + H5 passthrough 8192 + H6 idDe), handler ~11 (incl. timeout con tiempo virtual e id eco), respuesta texto ~6, emitter ~6; "sin android.*" en el extractor = verificación de CODE REVIEW, no test (H8); receiver validado manualmente en dispositivo (guion: Send Intent + Event Broadcast Received + adb am broadcast con paquete posicional o -n + caso negativo sin extras → sin crash/sin respuesta + notas de latencia cached state y quoting PowerShell — H11)
  - **T8 (build)**: sin dependencias Gradle nuevas (kotlinx-serialization-json ya es implementation en service/system — verificado por QA en Fase 1; AutoRemote sin SDK). Gate: `testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks`
- **ADR-015 (código)**: puente Tasker — Fase 3A, cierre del puente: allowlist real (F1), privacidad de respuesta (F2), URL callback AutoRemote (F3); UNA config compartida del puente (ver docs/DISENO-LOTE7-CIERRE-PUENTE.md):
  - **P1 (origen real)**: el origen deja de ser humo — el receiver obtiene `PendingResult.getSentUid()`/`getSentPackage()` (API 28+, guard minSdk 26; leídos tras goAsync y antes de finish; try/catch defensivo) → `OrigenTasker(uid?, paquete?)`; el seam CAMBIA a `handle(extra, OrigenTasker)` (anunciado en el KDoc del Lote 6; ~4 tests del handler adaptados). CORRECCIÓN documental: getSentUid/getSentPackage funcionan en broadcasts NO ordenados (la afirmación "solo en ordered" de ADR-014/T5 era FALSA) y devuelven el REMITENTE real (Tasker = net.dinglisch.android.taskerm; AutoRemote = com.bighugegiraffe.*; adb shell = uid 2000/paquete null; sistema = uid 1000; -1/null si no determinable). Deprecados en Android 16 por getSentFromUid/getSentFromPackage (misma semántica "initial sender"; nota para el futuro bump, no bloqueante con compileSdk 35)
  - **P2 (política allowlist)**: allow-all por defecto (cero regresión; el puente está en producción — 695 tests — y el control estructural pendiente es F5, no la allowlist) + allowlist OPT-IN fail-closed: activada → solo paquetes listados con **IGUALDAD EXACTA** (sin wildcards ni prefijos: listar `com.bighugegiraffe` NO deja pasar variantes no listadas — corrección H2; normalizados trim+lowercase; el UID es solo diagnóstico/log), DENY a shell/sistema/`OrigenTasker()` (no verificable → no permitido; **API<28 e indeterminable = MISMO input de test — M6**; la UI avisa "requiere Android 9+"). Error NUEVO aditivo `origen_no_autorizado` (TaskerBridgeContract, precedente error_timeout; mensaje "Error: Origen no autorizado." patrón ADR-009) emitido por el handler con las 6 claves e id eco best-effort, SIN ejecutar el bridge. Broadcast sin extras de no autorizado → silencio (H3 intacto). El flujo de voz NO se ve afectado (vía SystemCommandParser distinta; la allowlist vive solo en el canal broadcast)
  - **P3 (privacidad respuesta)**: `TaskerResponseEmitter.emitir(json, hablar)` SIN cambio de firma — el target viene de la config: `intent.setPackage(PuenteConfig.packageRespuesta)` si no-blank. DEFAULT = `net.dinglisch.android.taskerm` (fail-closed de privacidad: el extra `respuesta` con texto de notas deja de ser global). **Corrección H1: la materialización del default vive en `PuenteConfigStore.cargar()`** (clave ausente/blank → `PuenteConfig.PACKAGE_RESPUESTA_DEFAULT`; el `null` de la data class es solo valor CRUDO de guardado; el modo global no es alcanzable desde la UI en v1.1 — fuera de alcance). CORRECCIÓN documental: la afirmación "setPackage rompería AutoRemote" de ADR-014/T4 era FALSA — AutoRemote no consume TASKER_RESPONSE (solo re-emite la entrada; su salida es la URL callback); el riesgo real de setPackage es limitar otros consumidores legítimos (Tasker Beta `net.dinglisch.android.taskerm.beta`, MacroDroid...). Tasker Event "Broadcast Received" (registro dinámico bajo su package) SÍ recibe broadcasts con setPackage(taskerm)
  - **P4 (URL callback)**: endpoint HTTPS puro `https://autoremotejoaomgcd.appspot.com/sendmessage?key=..&message=..` (verificado en joaoapps.com/autoremote/direct; sin SDK; INTERNET ya en manifest de app; cero deps — HttpURLConnection JDK, no OkHttp: no está en el stack). Se envía SOLO si key no-blank Y origen con prefijo `com.bighugegiraffe.*` (**startsWith — el ÚNICO prefijo del diseño, decisión de canal de F3; F1 usa igualdad exacta — H2**; ADEMÁS del broadcast/TTS, nunca en lugar: los canales locales no se condicionan a la red); fire-and-forget en coroutine IO HERMANA del goAsync (no bloquea finish; pérdida por muerte de proceso = best-effort aceptado); fracaso de red → false + log, SIN reintento (WorkManager = YAGNI en ecosistema 1 dispositivo). Componentes: AutoRemoteUrlBuilder (puro) + UrlSender (interfaz) + HttpUrlSender (cáscara ~15 líneas) + AutoRemoteUrlCallback (orquestador puro testeable). `sender` (round-trip al escritorio) = opcional futuro, no prometido
  - **P5 (config compartida)**: UNA fuente `PuenteConfig` (allowlistActivada, paquetesPermitidos, packageRespuesta, autoRemoteKey) persistida en `PuenteConfigStore` (EncryptedSharedPreferences AES256 con fallback a prefs normales, patrón EXACTO ApiKeyProvider — la key es secreto de control). **Corrección H3: el store vive en `core:data/util` junto a ApiKeyProvider** (security-crypto es implementation de core:data y NO se propaga al compile de service:system → cero cambios de build; la constante del default de respuesta vive en `PuenteConfig`, no en TaskerBridgeContract). La leen TaskerMessageHandlerImpl (F1), TaskerResponseEmitterImpl (F2), AutoRemoteUrlCallback (F3) y la UI (PuenteSettingsSection patrón ApiKeySection en app/ui/puente, montada en MainActivity). Providers nuevos en AppModule (**5**: verifier, handler ampliado, emitter ampliado, urlSender, urlCallback — el store NO lleva @Provides, solo @Inject constructor @Singleton, vía única — O2)
  - **P6 (tests)**: ~48 tests JVM nuevos (verifier ~12, store ~6, handler +5 sobre 14 editados por la firma, emitter +3 sobre 6 editados por el constructor, URL builder ~5, URL callback ~8 — con advanceUntilIdle/runCurrent, M3 —, VM ~8-10 — M4); JUnit 4 + MockK + coroutines-test, SIN Robolectric ni dependencias nuevas (ADR-014/T8 intacto); supuestos declarados (M5: el test "fallback flag" asume que crypto lanza en JVM unit → catch); cáscaras (receiver, HttpUrlSender, store) sin unit tests → validación manual (guion del diseño: allowlist off/on con Tasker/adb shell, setPackage por defecto, URL callback con key). P5 incluye verificación de code review (O1): sin imports de TaskerOrigenVerifier/PuenteConfig fuera de bridge + core:data/util + app/ui/puente
  - **FUERA (Fase 3B)**: F5 (confirmación humana de irreversibles — cierra H2 estructuralmente), sender/round-trip al escritorio, reintentos de URL (WorkManager), compileSdk 36 (deprecación getSentUid), modo "respuesta SOLO por URL", modo "respuesta global" (sin setPackage — el campo vacío se materializa a taskerm, H1; requeriría un centinela explícito en la UI)
  - **ENMIENDA v1.2 (02/08/2026 — veto QA/Supervisor en el cierre: B1 + H1)**: el v1.1 fue implementado y pasó el gate (752 tests) pero el cierre lo VETÓ con fuentes: **B1** — `goAsync()` ejecuta `mPendingResult = null` y `getSentFromUid()/getSentFromPackage()` leen `mPendingResult` (AOSP BroadcastReceiver.java, android15-release) → llamarlas tras goAsync devuelve SIEMPRE -1/null; **H1** — la identidad del remitente solo se expone si el EMISOR hizo `BroadcastOptions.setShareIdentityEnabled(true)`, default FALSE (BroadcastRecord.java:489; BroadcastQueueModernImpl.java:1190-1220: `r.shareIdentity ? r.callingUid : Process.INVALID_UID`); doc oficial literal "if the sender opted in to sharing identity"; Tasker Send Intent/AutoRemote/adb shell NO activan el opt-in (no es configurable) → **la identidad del emisor NO es obtenible con API pública en nuestro escenario**, y los 57 tests del lote ejercitaban la capa pura con inputs que producción nunca produce (lección QA #7). **Supersede F1 y F3 del v1.1 (P1, P2, P4, P5, P6); P3 (F2) INTACTA**: P1' se ELIMINAN `OrigenTasker`/`obtenerOrigenDe`/`TaskerOrigenVerifier` (+15 tests: falsa seguridad — decisión sobre un input imposible) y el seam vuelve a `TaskerMessageHandler.handle(extra: String, token: String?)` (revierte la enmienda T6 del v1.1; regla B1 documentada: cualquier futura lectura de identidad DEBE ir en onReceive ANTES de goAsync). P2' F1 = **token compartido como extra `token` del INTENT** (NO campo del contrato JSON: el codec decodifica con `ignoreUnknownKeys=false` y tocar la base sealed de CommandEnvelope implicaría 22 overrides + validarBase + 95 tests de Fase 1 — EnvelopeSerializationTest 23, CodecTest 70, CorrespondenciaWireTest 2 — → **cero impacto en core/domain y en los 644 tests del contrato de Fase 1**; el token es autenticación de CANAL, no semántica de comando, igual que message/cmd). `EXTRA_TOKEN = "token"` en TaskerBridgeContract; `TaskerPayloadExtractor.extraer(message, cmd, token = null)` (los 19 tests existentes intactos por el default); validación en el handler (capa pura testeable): `tokenCompartido` no-blank → exige igualdad exacta tras trim → DENY = código NUEVO aditivo `token_invalido` (6 claves, id eco best-effort, bridge NO ejecutado, respuesta emitida — invariante H3); blank = canal abierto (default, compat modo actual, documentado "no protegido"). Seguridad honesta: el token viaja EN CLARO en el intent (modelo AutoRemote keys; cualquier app que espíe broadcasts lo ve), NO se loguea, el flujo de voz NO lo usa, F2 setPackage sigue protegiendo la respuesta y F5 sigue siendo el control estructural pendiente (comparación en tiempo constante descartada por honestidad: el token en claro la vuelve teatral). P4' F3 = flag explícito `enviarRespuestaURL: Boolean` (default OFF — cero llamadas URL sorpresa) + key: `AutoRemoteUrlCallback.responderSiAplica(respuestaJson: String)` decide SOLO por config (sin origen; el `startsWith("com.bighugegiraffe.")` muere con H1 y `PACKAGE_AUTOREMOTE_PREFIX` se elimina); la key y el flag son independientes del emisor real. P5' PuenteConfig v2 = `{packageRespuesta, autoRemoteKey, tokenCompartido, enviarRespuestaURL}` (ELIMINADOS allowlistActivada/paquetesPermitidos/normalizarPaquetes); `PuenteConfigStore` con las 4 claves (patrón EXACTO ApiKeyProvider intacto, core:data/util, H1 del default taskerm intacto); AppModule con **4 providers** (sin verifier). P6' = **45 tests del lote** (57 −15 verifier −5 allowlist-handler −4 urlCallback-origen −4 VM +6 token-handler +3 extractor-token +3 urlCallback-flag +2 VM +2 handler trim/case = 45; gate objetivo 740 CUMPLIDO (03/08/2026): BUILD SUCCESSFUL, 740 tests / 0 fallos (store 8, handler 22, extractor 22, emitter 9, urlBuilder 6, urlCallback 9, VM 8); Fase 1 (core/domain) SIN CAMBIOS; TaskerOrigenVerifierTest (15) ELIMINADO; cero referencias residuales a OrigenTasker/allowlist/origen_no_autorizado/PACKAGE_AUTOREMOTE_PREFIX; build/manifest sin cambios — v1.2a): ELIMINADO TaskerOrigenVerifierTest 15 y 4 tests del VM (T3/T4/T9/T10 — PuenteError y campo `error` ELIMINADOS, H1); EDITADOS store 8, handler 22 (14 revertidos a la firma nueva + 6 de token + 2 trim/case — H4), extractor 22 (19 intactos + 3), urlCallback 9 (0 INTACTOS — 6 conservados editados con `enviarRespuestaURL=true` explícito + 3 nuevos — H2), VM 8 (6 editados + 2 nuevos — H1); INTACTOS emitter 9 y urlBuilder 6; cáscaras (receiver, HttpUrlSender, store) → validación manual con el guion v1.2 (token erróneo → token_invalido; token correcto → ejecuta; flag ON → URL). La nota "compileSdk 36/deprecación getSentUid" queda obsoleta: getSentFromUid YA es la API actual y es inútil sin el opt-in del emisor (documentado en el FUERA de alcance del diseño v1.2). **ENMIENDA v1.2a (02/08/2026 — QA shift-left aplicado al diseño, hallazgos H1–H4; detalle en §12 de docs/DISENO-LOTE7-CIERRE-PUENTE.md)**: H1 (MAYOR) — "VM 10 → 10 editados" era imposible (T3/T4/T9/T10 usan `onAllowlistToggle`/`onPaquetesChange`/`PuenteError.ALLOWLIST_SIN_PAQUETES`, todo eliminado) → **VM 8** (6 editados + 4 ELIMINADOS + 2 NUEVOS: onUrlFlagChange reflejado antes de save y roundtrip refresh-tras-save); **PuenteError y el campo `error` del VM se ELIMINAN** (enum vacío sin entries = basura); neto VM = −2, no 0 (el "742" de QA asumía VM=10); H2 (MAYOR) — urlCallback **"0 INTACTOS"**: los 6 conservados se editan con `enviarRespuestaURL=true` explícito (sin él T1/T2 pasan por el early return del flag = falsos positivos, y T3/T8/T9/T10 fallarían); H3 (MENOR) — desglose real por archivo: store 8 + handler 8 + extractor 3 + emitter 3 + urlBuilder 6 + urlCallback 9 + VM 8 = 45; H4 (MENOR) — **+2 tests** de trim/case del token en el handler (22): extra " misecreto " con espacios → EJECUTA; "MiSecreto" vs config "misecreto" → `token_invalido` (case-sensitive). **Gate objetivo final: 740** (752 −24 +12 reconciliado: el −2 neto del VM se compensa con el +2 de H4; sin H4 sería 738)
- **ADR-005 (O3)**: hora=12 con período → "doce de la manana"=12:00, "doce de la noche"=0:00, "doce de la tarde"=12:00 (aprobado por producto; cambió el test "las doce de la manana es 0 00" → 12 00). "las cero de la noche"→12:00 aceptado como no-goal. Modificadores se aplican antes del período ("doce menos cuarto de la noche"→23:45)
- **ADR-006 (H4)**: separador '.' en phoneRegex (clase, no `\.?` — insuficiente para N puntos) y en la sanitización de CallNumber. Falsos positivos "6.30"/"600.123" aceptados (consistencia con separadores de espacio)
- **ADR-007 (O4)**: premisa corregida — las alarmas viven en la app Reloj (ACTION_SET_ALARM), sin PendingIntent ni API de cancelación. P1 = CancelAlarm + apertura de lista con guía TTS. Rama del parser con guard verbo+sustantivo (regex ^(cancela|...)\s+(la|las|mi|mis)\s+alarmas?) y precedencia sobre la rama 12; hora resuelta sobre `substringAfter("alarma")` (el anchor de findMatch capturaría el "la" del sustantivo). SUPERADO por ADR-008
- **ADR-008 (O4-P2)**: AlarmManager in-app como única vía (se abandona ACTION_SET_ALARM; decisión D1-D5 del diseño): (D1) setAlarm 100% in-app; (D2) disparo = notificación en canal propio `alarm_channel` (IMPORTANCE_HIGH), sin acoplar al overlay; (D3) persistencia Room v3 con migración EXPLÍCITA MIGRATION_2_3 (la destructiva queda solo como red de seguridad — QA: evitar pérdida de memories/cola); (D4) requestCode = hora*60+minuto (0..1439, una alarma por slot, FLAG_UPDATE_CURRENT); (D5) SCHEDULE_EXACT_ALARM, no USE_EXACT_ALARM (política targetSdk 35). Regla crítica: UNA única fábrica de PendingIntent (mismo requestCode+action+package+flags) compartida por set/cancel/restore — si divergen, cancel() es no-op y queda alarma fantasma (filterEquals ignora extras). Restore en app onCreate recomputa nextTrigger y purga pasadas; sin BootReceiver (decisión: tras reboot, abrir la app una vez restaura; BOOT_COMPLETED diferido). En API 31+ sin permiso, setExact lanza SecurityException → guard de permiso también en restore y orden setExact→upsert (TOCTOU)
- **ADR-009 (O7)**: convención única "Éxito: "/"Error: " (prefijo exacto, con espacio tras ':'), aplicada string a string en todas las acciones de service:system y en el parser ("ERROR:"→"Error:", el prefijo se antepone SIEMPRE en el parser a ActionResult.Error — ninguna acción duplica el prefijo en el cuerpo). UI: removePrefix("Éxito: ").removePrefix("Error: ") (encadenado cubre "Éxito: Error: X"). HELP_SPEECH y fallbacks del overlay sin prefijo. Regla de diseño: los mensajes no exponen excepciones crudas (p. ej. SearchAction normalizado)
- **ADR-B7 (Lote 8 — cierre B7)**: semántica del wire `estado` alineada con ADR-009: `ResultadoWire` (core/domain/bridge, JVM puro — no filtra android) con `estadoDe(mensaje)` = `"error"` SOLO si el mensaje de la acción empieza EXACTAMENTE por `"Error: "` (case-sensitive, prefijo exacto ADR-009; `"error: "` minúscula o `"Error de..."` sin ": " → ok) — los fallos FUNCIONALES dejan de reportar `estado="ok"` (contradicción con H4/S4 del ADR-013 que hacía que un guion Tasker filtrando por `%estado=="ok"` tratara fallos reales como éxito). Rama Success del bridge: `estadoDe==error` → `encodeResult("error", id, null, CODIGO_FALLO_ACCION="fallo_accion", mensaje VERBATIM)` con `resultado:null` LITERAL (código aditivo nuevo en TaskerBridgeContract, precedente error_timeout/origen_no_autorizado); si no → `ok` con resultado. La rama ActionResult.Error estructural sigue VERBATIM con `fallo_ejecucion` (excepciones crudas, ADR-009). Tests: `ResultadoWireTest` (5) + `TaskerBridgeTest` (4: "Error: ..." → fallo_accion verbatim + resultado:null, "error: " minúscula → ok, "Error de..." sin dos puntos → ok)
- **ADR-TMP-1**: duración = suma de TODAS las unidades (`findAll`), no regex compuesto con fallback
- **ADR-TMP-2**: "y media"/"y cuarto" tras horas se aceptan (fracción); "1 hora y 30" sin unidad y palabras numéricas de duración → diferidos (ambigüedad / limitación preexistente). DIFERIMIENTO SUPERADO por ADR-TMP-5/ADR-TMP-6
- **ADR-TMP-3**: diccionario de minutos sustituido por resolvedor numérico 0-59 (fracciones + simples 0-29 + decenas 20/30/40/50 + compuestos de 2 tokens); `HOUR_WORDS` intacto
- **ADR-TMP-4**: B3 estricto — token o combinación no resoluble → rechazo completo, nunca hora parcial silenciosa
- **ADR-TMP-5 (TMP-2)**: el dígito desnudo "X horas/minutos y N" es SIEMPRE minutos y se acepta si N ∈ 0..59 (elidido "minutos", no "segundos": el idioma natural omite la unidad de la magnitud dominante); lookahead negativo anti-doble-conteo `(?!\s*(?:\d|(?:horas?|minutos?|segundos?)))` — el `\d` adicional evita que `\d+` retroceda a un prefijo del número ("1 hora y 30 segundos" NO debe capturar "y 3" con trailing "0 segundos" → 60, no 63; igual "5 minutos y 10 segundos" → 5, no 6); trailing sin unidad se ignora ("1 hora y 30 y 5" → 90); N > 59 → null/Gemini ("1 hora y 60")
- **ADR-TMP-6 (TMP-num)**: SpokenNumber como vocabulario compartido (SIMPLE 0-29 + "un" coloquial para duraciones, TENS 20/30/40/50 con "veinte" duplicado, FRACTION media/cuarto). `quantityTokens` EXCLUYE fracciones (media/cuarto no son cantidad de unidad) y va ordenado por longitud desc (alternation sin solapes). Fracción solo ligada a horas ("media hora"/"cuarto de hora"/"X horas y media"); "X minutos y medio" residual → Gemini (ambiguo). Anti-doble-conteo fracción-hora: "1 hora y media hora" → 90 (la fracción compuesta lleva lookahead `(?!\s+(?:de\s+)?horas?)`; la suma la hace la fracción con unidad propia). Solo segundos → ceil a 1 minuto. Cantidad hablada no resoluble → null. "pasa una hora" se acepta como extensión del falso positivo "pasa " (preexistente). >59 hablado sin decena soportada ("sesenta y cinco minutos") → rechazo completo por residual (B1: `\b(?:sesenta|setenta|ochenta|noventa|cien|ciento)\s+y\s+(?:\d+|[a-z]+)\s+unidad`; sin él, durationSpokenUnitRegex saltaba la decena y capturaba "cinco minutos" → SetTimer(5) silencioso, violación de B3). Compuesto inválido con decena soportada ("veinte y diez minutos", token2 fuera de 1-9) → también rechazo por residual con exclusión de token2 válido. Patrones verbatim: `unsupportedTensRegex = \b(?:sesenta|setenta|ochenta|noventa|cien|ciento)\s+y\s+(?:\d+|[a-z]+)\s+(?:$UNIT_PATTERN)`; `invalidCompoundRegex = \b(?:veinte|treinta|cuarenta|cincuenta)\s+y\s+(?!(?:unitTokens)|(?:[1-9](?![0-9])))(?:\d+|[a-z]+)\s+(?:$UNIT_PATTERN)` (el lookahead excluye token2 válido 1-9 palabra o dígito de una cifra; sin lookbehind — rompería "una hora y cinco minutos"→65)
- **ADR-TMP-7 (MIN-dígito)**: token2 del compuesto "decena y unidad" acepta `\d{1,2}` con la MISMA validación 1..9 que las palabras ("veinte y 5" → 25, "veinte y 10" → null, "media y 5" → null): cierra el hueco donde "y 5" residual se descartaba silenciosamente contra el espíritu de B3 (rechazo completo). Extracción a SpokenNumber compartido (resolve añade el path token2-dígito; no es refactor 1:1 puro)
- **ADR-010 (O6)**: el trim de espacios se hace en `parse()` (`normalize(text).trim()`), NO en normalize (su contrato de longitud invariante se mantiene para los índices de TimePhraseParser) NI en la UI. Invariante de alineación: `trimmed` y `original.trim()` van ambos trimados → los prefijos ASCII coinciden en longitud y posición, y `substring(prefix.length)` cae en el carácter correcto. PRECISIÓN M3: `original.trim()` NO elimina ¿/¡ (no son whitespace) — el trim solo alinea los ESPACIOS de borde; los ¿/¡ residuales del original los absorben la limpieza de bordes del argumento (M2, misma lista que cleanNoteText, aplicada en extractAfterPrefix) y cleanNoteText (rama 0, offset de 1 char absorbido por el espacio colgante + trimEnd). Caso teórico de la rama 12: `matchEnd` de trimmed vs `text.indexOf("para ", …)` del original divergen solo con k≥~10 espacios iniciales (imposible por voz) → aceptado
- **ADR-011 (H3+H5)**: desambiguación del ':' según el contexto que lo precede: (a) dígito-:-dígito = hora ("19:45", rama alarma; ':' queda intacto en normalize a propósito); (b) palabra-: = separador de comando (ramas 2 y 10 con variantes `verbo:` y `trimStart(':')` para limpiar "busca : gatos"/"busca: : gatos"). El orden de prefijos de la rama busca es IRRELEVANTE — "busca " y "busca:" son disjuntos en la posición 5 (' ' vs ':'), a diferencia de NOTE_PREFIXES (anidados). HELP: lookahead ampliado `(?=\s|:|$)` (H5) con falso positivo aceptado "piensa que puedes hacer: X" (patrón poco natural por voz, marginal)
- **ADR-001**: notas = archivos de texto plano en `filesDir/notas/{epochMillis}.txt` UTF-8 (sin Room/DataStore; cero permisos; borrado con "borrar datos de la app" aceptado)
- **ADR-002**: límite de notas = truncar con aviso TTS (`take(1000)` en NoteAction; aviso solo si >1000; corte a mitad de palabra y surrogate pairs aceptados). ENMENDADO (N-OBS5): el truncado avanza por CODE POINTS (`truncateByCodePoints` — `Character.charCount(codePointAt(idx))`), nunca parte un surrogate pair; el aviso refleja el corte real (`finalText.length < text.length`; un emoji que "cabe" no avisa)
- **ADR-003**: alcance create-only; consulta diferida (N1/N2); si aparece un segundo consumidor, extraer `NoteRepository` (interfaz en domain, impl en service:system). ENMENDADO (N1): la consulta ya existe (readNotesSummary/readNote en NoteAction); la normalización local de readNote (`normalizeLocal`) se acepta duplicada hasta un TERCER consumidor; el regex `^\d{13}\.txt$` filtra archivos ajenos al formato. ENMENDADO (M23, Lote 10): la justificación "core:domain no debe filtrarse a service:system" era FALSA — service:system SÍ depende de core:domain por diseño (SystemCommandParser, SystemCommand, SystemAction, ActionResult…); la duplicación de `normalizeLocal` está justificada por CONTRATO DISTINTO (normalize del parser = longitud invariante con ':' conservado para TimePhraseParser/extractAfterPrefix; normalizeLocal = colapso de espacios + trim para MATCH por substring). Umbral del 3er consumidor NO alcanzado (hoy 2: parser.normalize + NoteAction.normalizeLocal; GeminiFunctionCatalog/TaskerMessageHandler no normalizan) → documentar, no refactorizar
- **ADR-004**: rama de notas con precedencia máxima (antes de ayuda/temporizador/alarma, que usan `contains`); prefijos ordenados "de/del " → ": " → ":" → " ". ENMENDADO (N-OBS2+N1): nuevo orden "de " → "de:" → "del " → ": " → ":" → " " ("de:" DEBE ir antes del " " desnudo: si no, "crea una nota de: pan" → nota "de: pan"); INVARIANTE N-OBS3 formalizado — TODO prefijo de nota (crear y leer) termina en espacio o ':'; las excepciones colgantes "de"/"del" sin argumento se filtran por el guard → Gemini. La rama de lectura (0b) hereda la misma precedencia sobre ramas contains
- **ADR-012 (N1)**: lectura de notas. (1) Resumen siempre ("lee mis notas") vs lectura por coincidencia truncada a 400 con aviso de longitud ("lee la nota de X") — paginación diferida a P4/MULTI. (2) `^\d{13}\.txt$` filtra archivos ajenos (README, etc.) — formato {epochMillis}.txt ya preparado para ordenar por timestamp (desc = más reciente). (3) Substring sobre contenido NORMALIZADO localmente (falso positivo aceptado por diseño: "pan" matchea "españa"; NO usar match de palabra: rompería tildes/ñ). (4) Igualdad EXACTA en "lee mis notas" ("lee mis notas:" también): "lee mis notas de la semana" → Gemini (el lookahead (?=\s|:|$) de la rama 1 no sirve: el espacio lo atraviesa). (5) Precedencia de la rama 0b sobre ramas 3/12 ("lee la nota de la alarma de las 7" → ReadNote). (6) Extracto unificado con saveNote: "Empieza así: «{excerpt}»" (mayúscula, misma frase) y plural "La más reciente empieza así". (7) N2 (FileProvider) DIFERIDO: sin pantalla propia de notas no compensa — diseño de referencia pendiente

## Notas de proceso

- **GATE MECANIZADO (Lote 12, M13)**: comando canónico `gradlew gate --rerun-tasks` (unit tests 5 módulos + assembleDebug + JaCoCo enforcement: domain 0.86, system 0.60, data 0.57, overlay 0.32, :app report-only). `testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks` queda como subconjunto; `:app:connectedDebugAndroidTest` (androidTest +8, solo con dispositivo).
- Compilation Gate obligatorio antes de entregar: `testDebugUnitTest :core:domain:test :app:assembleDebug --rerun-tasks` (equivalente JVM al gate; usar `gradlew gate` para el cierre completo)
- Quirk Gradle/Dagger: usar `--rerun-tasks` ante errores Dagger raros (KSP incremental enmascara ciclos)
- MockK 1.13.12: métodos void sin stub en mock no-relaxed → `no answer found` → `every { ... } returns Unit` obligatorio
- mockkStatic(PendingIntent) sobre android.jar stub: riesgo de verdes falsos con isReturnDefaultValues → verify EXPLÍCITO de getBroadcast en cada test; `anyConstructed` no existe en 1.13.12 → slot capture para capturar args de Intents construidos con mockkConstructor
- AlarmManager no se mockea: se envuelve en AlarmScheduler inyectable (patrón VolumeController); AlarmReceiver (goAsync+Hilt) no es testeable sin Robolectric → cáscara fina, lógica en AlarmAction.onAlarmFired
- AudioManager: métodos FINAL en API 35 → no mockeable → abstracción `VolumeController`
- MockK: `any()` DENTRO del constructor de una data class en coVerify/coEvery (p. ej. `execute(SystemCommand.SearchGoogle(any()))`) no matchea ("left matchers") → usar el matcher de tipo sobre el argumento completo (`any<SystemCommand.SearchGoogle>()`) o confiar en el stub tipado (ejecutar otra acción → MockKException "no answer found" = rojo, aserción fuerte por sí sola)
- android.jar con returnDefaultValues en JVM unit: `Uri.encode`/`Uri.parse` devuelven null sin lanzar → mockkStatic(Uri) y emular encode con URLEncoder (espacio→%20, &→%26, idéntico a Uri.encode)
- Icono launcher: degradado legacy GDI+ con angle=45 (muestreo: 135=sup-der, 315=inf-izq, 45=sup-izq)
